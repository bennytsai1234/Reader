# 0.2.42 (2026-04-29)

- 清理 reader runtime 殘留分支，移除未接入 `ReaderPage -> ReaderRuntime -> Viewport` 主線的舊 controller、coordinator、restore、progress store、navigation 與 viewport mailbox 檔案。
- 移除舊 runtime TTS engine suite，保留目前實際使用的 `ReaderTtsController -> TTSService -> TTS highlight overlay` 路徑。
- 精簡 `ReaderSessionFacade` 與 `ReaderDisplayCoordinator`，只保留目前 reader 主線仍使用的加書架與顯示格式化接口。
- 統一 reader anchor / open target 使用 `features/reader/engine/ReaderLocation`，避免 runtime 內保留另一份同名位置模型。
- 整理 reader 設計文檔，將歷史討論稿移到 `docs/archive/`，新增目前 reader 狀態、手機實測計劃與資料夾歸屬規則。
- 驗證 `flutter test test/features/reader` 與 `flutter analyze` 通過。
