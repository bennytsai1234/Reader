# 0.2.30 (2026-04-27)

這個版本補齊 line-first 閱讀器改造後的章節導航與翻頁邊界，讓目錄跳章、上一章/下一章按鈕、章節滑桿，以及 slide 模式跨章翻頁都回到同一套 `chapterIndex + charOffset` / PageGroup 定位模型。

## 閱讀器

- 修正目錄選擇章節時，重新選目前章也會回到章首
- 修正底部上一章/下一章與章節滑桿，改以實際顯示章節為基準
- 修正 slide 模式跨章後重建 slide window 時可能跳回錯誤頁面的問題
- slide recenter 改用 `chapterIndex + charOffset` 找回 PageGroup，不再依賴舊 `pageIndex`
- slide 下一頁跨章時接到下一章章首，上一頁跨章時接到上一章章末

## 資料庫

- 重置 reader database schema 為新架構的初始版本
- 移除舊版相容 migration 與舊 durable 欄位 fallback
- 閱讀進度只保留 `chapterIndex + charOffset` 作為 durable 座標

## 驗證

- `flutter test test/features/reader/reader_content_pipeline_test.dart test/features/reader/reader_runtime_flow_test.dart test/features/reader/read_book_controller_test.dart test/features/reader/reader_page_viewport_bridge_test.dart test/features/reader/reader_page_compile_test.dart`
- `flutter analyze`
