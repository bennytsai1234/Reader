# 應用殼層 (app_shell)

## Responsibility
- 擁有應用程式的生命週期殼層：進入點 `lib/main.dart:58` 的 `main()` / `_startApp()`、全域 `runZonedGuarded` 與 `ErrorWidget.builder` 錯誤兜底、`FlutterNativeSplash.preserve` 啟動屏交棒、以及 `ReaderApp` / `_AssociationLifecycleHost` 的頂層 `MaterialApp` 組裝。
- 擁有依賴注入與全域狀態的組裝邊界：`lib/core/di/injection.dart:30` 的 `configureDependencies()` 集中註冊 `GetIt` 單例（`Logger`、`AppDatabase`、全部 DAO、`NetworkService`、`TTSService`、`SharedPreferences`），並由 `lib/app_providers.dart:14` 的 `AppProviders.providers` 將 `BookshelfProvider`、`SettingsProvider`、`ThemeSettingsProvider`、`ChangeCoverProvider`、`DownloadService`、`TTSService`、`ExploreProvider` 注入 `MultiProvider`。
- 擁有全域配置的靜態鏡像與常量契約：`lib/core/config/app_config.dart:4` 的 `AppConfig`（`replaceEnableDefault`、`readerPageAnim`、`readerLastLineSpacingCompensation`、`readerV2ContentJustify`）作為 Model 層可同步讀取的預設值；`lib/core/constant/prefer_key.dart:2`、`book_type.dart:1`、`source_type.dart:1`、`app_pattern.dart:2` 定義全域 `SharedPreferences` Key、書籍類型位元遮罩、書源類型、以及 JS/圖片/dataURI 等正則。
- 擁有 Provider 基類契約：`lib/core/base/base_provider.dart:8` 的 `BaseProvider` 規範 `isLoading`/`errorMessage`/`lastError`、`CancelToken`、`runTask<T>()` 自動處理 loading 與 `DioException` 取消、`dispose()` 時取消請求。
- 擁有設計系統與導航基建：`lib/shared/theme/*`（`app_tokens.dart:4` 的 `AppPalette`/`AppSpacing`/`AppRadius`、`app_text_styles.dart:4` 的排版階、`custom_app_theme.dart:7` 的 `buildAppTheme()` 動態生成 `ThemeData`、`theme_customization.dart:9` 的 `AppUiThemeColors`/`ReaderAreaThemeColors` 可序列化主題、`app_theme.dart:9` 的舊版 `AppTheme` + `ReadingTheme` 及其 `readConfig.json` 持久化）；`lib/shared/navigation/app_route_observer.dart:3` 的全域 `appRouteObserver` 與 `lib/shared/navigation/book_open_route.dart:14` 的 `BookOpenRoute`（淡入+上滑 280ms/220ms）作為閱讀器轉場的唯一入口；`lib/shared/widgets/app_bottom_sheet.dart:5` 與 `source_option_tile.dart:5` 提供全域底部工作表與書源選項瓦片。
- 擁有主導航殼與啟動編排：`lib/features/welcome/main_page.dart:38` 的 `MainPage`（`_defaultDestinations` 三頁籤：書架/發現/我的，`PageView` + `NavigationBar` + `_KeepAliveWrapper`，雙擊刷新、返回鍵二次確認退出、空書架 FAB 導向 `SearchPage`、啟動疊層與 `DefaultData.initDeferred` / `UpdateCheckRunner.runAutomatic` 的首幀後調度）；`lib/features/welcome/startup_failure_panel.dart:6` 的 `StartupFailurePanel` 作為 `configureDependencies` 失敗時的降級 UI。
- 擁有外部關聯（association）與關於頁的殼層能力：`lib/features/association/association_handler_service.dart:17` 的單例 `AssociationHandlerService`（mix `UriAssociationHandler`/`FileAssociationHandler`/`AssociationDialogHelper`）處理 `legado://`/`yuedu://` 深連結與 `receive_sharing_intent` 檔案/文字分享；`lib/features/about/about_page.dart:9`、`crash_log_page.dart:8`、`update_dialog.dart:9`、`update_check_runner.dart:10` 提供關於、崩潰日誌查看/清除、更新檢查對話框。
- 未來任務若涉及：新增全域 Provider、調整啟動時序或 Splash 策略、修改主題 token/色彩、新增底部導航分頁、新增深連結 scheme 或分享類型、調整全域錯誤/日誌/崩潰策略，應從此模組開始。

## Scope
- **代表性資料夾與檔案**：
  - 進入點與組裝：`lib/main.dart`（`runBackgroundTask<T>`、`callbackDispatcher`、`scaffoldMessengerKey`/`rootNavigatorKey`、`kAppDisplayName`、`_startApp`、`_retryCriticalStartup`、`_StartupFailureApp`、`_runPostFirstFrameStartupTasks`、`_cleanupLegacyCustomFontArtifacts`、`ReaderApp`、`_AssociationLifecycleHost`）、`lib/app_providers.dart`（`AppProviders.providers`）。
  - DI：`lib/core/di/injection.dart`（`getIt` 單例、`configureDependencies` 註冊順序與 `Future.wait` 初始化）。
  - 全域配置鏡像：`lib/core/config/app_config.dart`（靜態欄位由 `SettingsProvider` 與 `ReaderSettingsMixin` 同步）。
   - 常量：`lib/core/constant/prefer_key.dart:2`（195 個 `static const String` Key，含 `PreferKey.reader*`、`exploreLast*`、`coverSearchPriority` 等）、`lib/core/constant/book_type.dart:1`（位元 `0x08`/`0x10`/`0x40`/`0x80`/`0x100`/`0x200`/`0x400` 與 `isImage`/`isLocal`/`isOnShelf`）、`lib/core/constant/source_type.dart:1`（`book=0`/`audio=1`/`image=2`/`file=3`，`rss` 已棄用）、`lib/core/constant/app_pattern.dart:2`（`jsPattern`/`expPattern`/`imgPattern`/`dataUriRegex`/`bookFileRegex` 等）。
  - 基類：`lib/core/base/base_provider.dart`（`BaseProvider`）。
  - 共享 UI/導航：`lib/shared/theme/custom_app_theme.dart`（`buildAppTheme`）、`lib/shared/theme/app_theme.dart`（`AppTheme` + `ReadingTheme` JSON 序列化）、`lib/shared/theme/app_tokens.dart`（`AppPalette` 紙/墨色板、`AppSpacing`、`AppRadius`）、`lib/shared/theme/theme_customization.dart`（`AppUiThemeColors`/`ReaderAreaThemeColors` 含 `toJson`/`fromJson` 與 `_readColor` 容錯）、`lib/shared/theme/context_ext.dart:6`（`BuildContext.warning/success/danger/info`）、`lib/shared/theme/app_text_styles.dart:4`（`display` ~ `titleSm` 字階）；`lib/shared/navigation/app_route_observer.dart`、`lib/shared/navigation/book_open_route.dart`；`lib/shared/widgets/app_bottom_sheet.dart`（`AppBottomSheet.show` 封裝 `showModalBottomSheet`，`SheetSection`）、`lib/shared/widgets/source_option_tile.dart`。
  - 歡迎/殼導航：`lib/features/welcome/main_page.dart`（`MainPage`/`MainDestination`/`_KeepAliveWrapper`）、`lib/features/welcome/startup_failure_panel.dart`（`StartupFailurePanel`）。
  - 關於：`lib/features/about/about_page.dart`（`AboutPage` 含 GitHub/許可證/免責聲明/檢查更新/崩潰日誌入口）、`lib/features/about/crash_log_page.dart`（支援 `readLogs`/`clearLogs`/`writeClipboard` 注入）、`lib/features/about/update_dialog.dart`（`UpdateDialog` + `UpdateDialogResult`）、`lib/features/about/update_check_runner.dart`（`UpdateCheckRunner`）、`lib/features/about/external_url_launcher.dart:6`（`launchExternalUrlWithFeedback`）。
  - 關聯：`lib/features/association/association_handler_service.dart`（`AssociationHandlerService` 單例）、`lib/features/association/handlers/association_base.dart:6`（`AssociationBase` 定義 `appLinks`/`linkSubscription`/`sharedMediaSubscription`）、`uri_association_handler.dart:16`（`UriAssociationHandler` 解析 `legado://import/*?src=` 與 `yuedu://*/importonline?src=`）、`file_association_handler.dart:14`（`FileAssociationHandler` 處理 `.json` 與 `isSupportedLocalBookExtension` 本地書）、`association_dialog_helper.dart:16`（`AssociationDialogHelper` 的 `showImportDialog`/`showForceImportDialog`/`_importReplaceRules`）。
   - 平台與資源：`pubspec.yaml:4`（`name: night_reader`、`version: 0.2.146+160`、`flutter_launcher_icons` 深棕 `#1A1612` 底板）、`flutter_native_splash.yaml:6`（亮色 `#F4EFE3`/暗色 `#1A1612` 純色原生 splash，`android_12` 靜態 `@drawable/splash_icon`）、`assets/app-icon.png`/`assets/app_icon/ic_foreground.png`/`assets/ui/app_icon.webp`、`assets/default_sources/sources.json`、`assets/opencc/*`、`android/app/src/main/AndroidManifest.xml:12`（`MainActivity` `singleTop`、`NormalTheme`/`EnableImpeller=false`、`AudioService`/`MediaButtonReceiver`、`queries` 含 `TTS_SERVICE`/`PROCESS_TEXT`）、`ios/Runner/Info.plist:10`（`CFBundleDisplayName 夜讀`、`UIBackgroundModes audio`、`NSPhotoLibraryUsageDescription`）。
- **公開 API / 進入點**：`main()` → `_startApp()` → `ReaderApp` → `MainPage`；`BookOpenRoute(book, openTarget, initialChapters)`；`AppProviders.providers`；`configureDependencies()` 與 `callbackDispatcher()`（Workmanager 背景入口）；`appRouteObserver`；`ThemeSettingsProvider` 的靜態 `resolveReaderAreaColors`/`resolveReaderTheme`/`areaMode`/`saveAreaMode`；`AssociationHandlerService().init(context)`；`launchExternalUrlWithFeedback`。
- **命令與測試**：`flutter pub get` / `flutter analyze` / `flutter test`（見 `AGENTS.md` 發布流程）；發布由 `.github/workflows/android-release.yml` 在 `v*` tag 或 `workflow_dispatch` 觸發。掃描時未發現 `test/` 下針對 app_shell 的專屬單元測試；`CrashLogPage` 透過建構子注入可測試（`readLogs`/`clearLogs`/`writeClipboard`），但無對應測試檔案提交。

## Dependencies & Impact
- **上游依賴**：
  - 套件：`provider ^6.1.5+1`（全域狀態）、`get_it ^9.2.1`（DI）、`shared_preferences ^2.5.5`（`PreferKey` 與主題持久化，`injection.dart:100` 預載以消除首幀閃爍）、`flutter_native_splash ^2.4.8`（啟動屏）、`workmanager ^0.10.7`（`callbackDispatcher` 背景任務，`main.dart:43`）、`app_links ^7.2.1` + `receive_sharing_intent ^1.9.0`（深連結/分享）、`package_info_plus ^10.2.1`（`AboutPage`/`CrashHandler`）、`url_launcher ^6.3.2` + `share_plus ^13.3.0`（外部連結）、`drift ^2.34.3` + `path_provider`（透過 `AppDatabase` 間接依賴，`injection.dart:47` 註冊）、`logger ^2.7.0`（全域 `Logger`）、`cached_network_image`/`image_picker`/`permission_handler` 等由下游頁面間接觸發。
   - 內部：`core/services/app_log_service.dart:8`（`AppLog` 記憶體佇列 100 條 + `kDebugMode` 下轉發 `Logger`）、`core/services/crash_handler.dart:11`（`CrashHandler.init` 攔截 `FlutterError.onError` 與 `PlatformDispatcher.onError`，寫入 `getExternalStorageDirectory` 或 `getApplicationDocumentsDirectory` 的 `crash-*.log`）、`core/services/default_data.dart:34` 的 `DefaultData.initDeferred` 延遲初始化、`core/services/update_service.dart:11` + `update_ignore_store.dart`（更新檢查）。
- **下游影響**：
  - 所有 `features/*` 的 `Provider` 皆透過 `AppProviders` 掛載；若調整 `injection.dart` 註冊順序或 `app_providers.dart` 的 `lazy:false`（如 `ExploreProvider`）會直接影響首頁發現頁的 DB 訂閱時序與首幀效能。
  - `AppConfig` 的靜態欄位被 `features/reader*` 與 `core/models/book_extensions.dart` 讀取；修改其預設值或同步點（`SettingsProvider.setReplaceEnableDefault:95`、`ReaderSettingsMixin`）需同時更新對應讀取端，否則出現 Model 層與持久化不一致。
  - `PreferKey` 是全工作區的字串契約，數十個 `features/settings/*` 與 `core/*` 直接以字面量讀寫；新增或重新命名 key 必須全域搜尋並考慮舊版資料遷移。
  - `AppPalette`/`AppTokens`/`buildAppTheme` 是所有頁面的視覺基線；改動色板或 `ColorScheme` 生成邏輯會波及 `custom_app_theme.dart:8` 的 `primaryContainer`/`secondaryContainer` 與 `cardTheme`/`dialogTheme` 等全域主題。
  - `BookOpenRoute` 是閱讀器唯一轉場，若改動 `transitionDuration` 或動畫曲線，需與 `ReaderV2Page` 的 `_openRuntimeAfterFirstFrame` 首幀後重排版協同，否則可能掉幀。
  - `AssociationHandlerService` 的對話框直接呼叫 `SourceImportService`、`BookshelfExchangeService`、`ReplaceRuleDao`；改動其 `mixin` 介面或 `showImportDialog` 的 `type` 映射（`bookSource`/`replaceRule`/`book`/`auto`）會影響書源/替換規則/書架匯入的端到端鏈路。

## Key Flows
- **應用啟動與錯誤降級**：`main.dart:58` `main()` → `runZonedGuarded(_startApp)` → `WidgetsFlutterBinding.ensureInitialized` → `FlutterNativeSplash.preserve` → 設定 `ErrorWidget.builder`（渲染錯誤寫 `CrashHandler.recordFlutterError` 並顯示黑底錯誤頁） → `await configureDependencies()` → 設定 `FlutterError.onError` 轉發 `CrashHandler` → `runApp(MultiProvider(... ReaderApp))` → `addPostFrameCallback(_runPostFirstFrameStartupTasks)`。若 `configureDependencies` 拋錯，捕獲後 `runApp(_StartupFailureApp)` 顯示 `StartupFailurePanel`（重試觸發 `_retryCriticalStartup` → `getIt.reset()` → `_startApp`）。
- **DI 初始化**：`injection.dart:30` `configureDependencies` 依序 `registerSingleton<Logger>` → `registerSingleton<AppDatabase>` → `registerLazySingleton` 全部 DAO（`BookDao`/`BookSourceDao`/`ChapterDao`/`BookGroupDao`/`BookmarkDao`/`CacheDao`/`CookieDao`/`DownloadDao`/`ReadRecordDao`/`ReplaceRuleDao`/`RuleSubDao`/`SearchBookDao`/`SearchHistoryDao`/`SearchKeywordDao`/`KeyboardAssistDao`/`ServerDao`/`ReaderChapterContentDao`） → `registerSingleton<NetworkService>`/`TTSService` → `await SharedPreferences.getInstance` 並註冊 → `Future.wait([CrashHandler.init, NetworkService.init, TTSService.init])`。
- **主題應用**：`main.dart:235` `ReaderApp` 以 `Consumer2<SettingsProvider, ThemeSettingsProvider>` 監聽 `SettingsProvider.themeMode/locale` 與 `ThemeSettingsProvider.effectiveAppLight/effectiveAppDark` → 呼叫 `buildAppTheme(AppUiThemeColors, Brightness)` 生成 `theme`/`darkTheme`（含 `primary`/`secondary`/`surface`/`textPrimary`/`textSecondary`/`border` 的語意映射與 `AppBar`/`Card`/`Dialog`/`NavigationBar` 等組件主題）→ `MaterialApp(themeMode, locale, navigatorKey, scaffoldMessengerKey, navigatorObservers:[appRouteObserver])`。
- **首幀後任務與 Splash 釋放**：`main.dart:185` `_runPostFirstFrameStartupTasks` 在 `kDebugMode` 下強制 `prefs.setBool('recordLog', true)`，然後 `unawaited(_cleanupLegacyCustomFontArtifacts)`（刪除 `selected_font_family` 與 `documents/fonts/`）、`await Workmanager().initialize(callbackDispatcher)`；並行於 `MainPage:85` 的 `initState` → `addPostFrameCallback` → `DefaultData.initDeferred()` 與 `_releaseSplashWhenShelfReady()`：記錄 `_splashHeldAt`，若 `BookshelfProvider.isLoading` 則監聽並設 2 秒超時 `_splashShelfTimeout`，超時後先顯示 `正在載入書架…` 遮罩再 `FlutterNativeSplash.remove()`，否則滿足 `900ms` 最小顯示 `_splashMinDisplay` 後移除原生 Splash；`_detachSplashShelfListener` 確保監聽器釋放。
- **主導航與返回**：`MainPage:120` `PopScope(canPop:false)` 攔截返回 → `_handleBackIntent`：若非書架分頁則 `animateToPage(0, 250ms, easeInOut)`，否則 2 秒內二次返回才 `SystemNavigator.pop()`，期間 `SnackBar('再按一次退出')`；分頁切換由 `PageController.animateToPage` 驅動，`_KeepAliveWrapper` 保活三個子頁；雙擊當前分頁觸發 `onDestinationDoubleTap`（預設書架分頁雙擊 `BookshelfProvider.loadBooks()`）；空書架時顯示 `FloatingActionButton` 跳 `SearchPage`，載入錯誤時全屏覆蓋顯示 `loadErrorMessage` 與重試按鈕。
- **深連結與分享接收**：`main.dart:261` `_AssociationLifecycleHost` 在 `didChangeDependencies` 首幀後 `AssociationHandlerService().init(context)` → `association_handler_service.dart:32` 同時訂閱 `AppLinks.uriLinkStream` → `handleUri` 與 `ReceiveSharingIntent.instance.getMediaStream`/`getInitialMedia` → `handleSharedMedia`；`uri_association_handler.dart:17` 辨識 `legado://import/{bookSource|replaceRule|addToBookshelf}?src=`（`host==import`）與 `legado://addBook?url=`，以及 `yuedu://booksource|replace/importonline?src=` 舊版相容，缺 `src` 直接返回，否則 `showImportDialog(context, mappedType, src)`；`file_association_handler.dart:15` 遍歷 `SharedMediaFile`，`.json` 進 `_handleSharedFile`（讀檔 `jsonDecode` 依首元素是否含 `bookSourceUrl`/`pattern`/`themeName` 判 `type`，再 `showImportDialog(isFile:true)`，解析失敗則 `showForceImportDialog`），本地書副檔名經 `isSupportedLocalBookExtension` 進 `handleSharedBook`（複製到 `documents/InkpageBooks/<basename>`，再 `BookshelfProvider.importLocalBookPath`）；`association_dialog_helper.dart:17` 的 `showImportDialog` 依 `type` 動態展示「匯入書源/書架/替換規則」按鈕，分別呼叫 `SourceImportService.importFromJson/importFromUrl`、`BookshelfExchangeService.importFromFile`/`importBookshelfFromUrl`、`_importReplaceRules`（`jsonDecode` 列表 → `ReplaceRule.fromJson` → `ReplaceRuleDao.upsert`），按鈕以 `_btn` 包裝 try/catch 並以 `SnackBar` 回饋。
- **背景任務**：`main.dart:27` `runBackgroundTask<T>` 為可測試的純函式（注入 `initialize`/`loadBookshelf`/`logInfo`），`callbackDispatcher:42` 以 `@pragma('vm:entry-point')` 標記供 Workmanager 呼叫，`Workmanager().executeTask` 內執行 `configureDependencies` + `BookDao.getInBookshelf` 並記錄 `Checking updates for N books`。
- **更新檢查**：`MainPage:346` ` _runAutomaticUpdateCheck` 延遲 500ms 後 `UpdateCheckRunner().runAutomatic(() => context)`；`update_check_runner.dart:21` 的 `runAutomatic` 僅在 `Platform.isAndroid` 執行，`checkLatest` 取 `UpdateInfo` 後比對 `UpdateIgnoreStore.isIgnored(tagName)`，未忽略則 `showDialog(UpdateDialog)`，結果 `ignored` 時寫入忽略；手動路徑 `AboutPage:233` `_checkUpdate` → `runManual` 回 `UpdateCheckOutcome`（`shown`/`upToDate`/`failed`/`dismissed`/`notSupported`）並以 `SnackBar` 提示；`UpdateDialog:14` 的「前往下載」以 `launchExternalUrlWithFeedback` 開啟 `releasePageUrl` 或 `downloadUrl`。
- **崩潰日誌與外部連結**：`CrashHandler:31` `init` 預載 `PackageInfo` 並重寫 `FlutterError.onError`/`PlatformDispatcher.onError`（`_shouldAbsorb` 過濾 `CannotDeliverBroadcastException` 等），`_saveLog` 寫 `crash-*.log` 與 `crash_log.txt` 含裝置與記憶體資訊，`readLogs` 聚合最近 20 檔；`CrashLogPage:171` 支援剪貼簿與清除確認；`external_url_launcher.dart:6` 以 `url_launcher` `LaunchMode.externalApplication` 開啟，失敗時提供「複製連結」`SnackBarAction`。

## Change Entry Points & Routes
- **新增或調整全域初始化順序**：`lib/core/di/injection.dart:30` `configureDependencies`（註冊順序、是否 `lazy`、是否加入 `Future.wait`）→ `lib/main.dart:113` `_startApp` 的 `await configureDependencies()` 呼叫點 → `lib/app_providers.dart:14` `AppProviders.providers`（如需把新 `ChangeNotifier` 掛到全域）→ `lib/core/services/crash_handler.dart:15` `CrashHandler.init`。多檔案路徑：`injection.dart` ↔ `main.dart` 的 `_retryCriticalStartup`（`getIt.reset`）↔ `StartupFailurePanel` 降級頁必須同步。
- **修改啟動屏/首幀時序**：`lib/main.dart:66` `FlutterNativeSplash.preserve` + `lib/features/welcome/main_page.dart:265` `_releaseSplashWhenShelfReady`（`_splashMinDisplay`/`_splashShelfTimeout`/`_showStartupLoadingOverlay`）+ `flutter_native_splash.yaml:6` 的 `color`/`color_dark`/`android_12`。重跑 `dart run flutter_native_splash:create` 後需手動補回 `android/app/src/main/res/values*/styles.xml` 的 `android:windowSplashScreenAnimatedIcon`（見 yaml 註解）。
- **增刪底部導航分頁**：`lib/features/welcome/main_page.dart:17` `_defaultDestinations`（`MainDestination` 含 `icon`/`selectedIcon`/`label`/`page`）→ `MainPage.destinations` 建構參數 → `_PageController`/`NavigationBar`/`_KeepAliveWrapper`。新增分頁需同時考慮 `_handleBackIntent` 的「先回首頁」邏輯與雙擊回調 `MainDestinationDoubleTapCallback`。
- **主題/設計系統調整**：`lib/shared/theme/app_tokens.dart:4` `AppPalette`/`AppSpacing`/`AppRadius` → `lib/shared/theme/theme_customization.dart:9` `AppUiThemeColors`/`ReaderAreaThemeColors`（含 `copyWith`/`toJson`/`fromJson`）→ `lib/shared/theme/custom_app_theme.dart:7` `buildAppTheme`（`ColorScheme.fromSeed` 與 `primaryContainer` 透明度、文字主題映射）→ `lib/features/settings/theme_settings_provider.dart:9` 持久化 key（`theme_app_*_v1` 等）與 `effectiveAppLight/effectiveAppDark`。舊版 `lib/shared/theme/app_theme.dart:9` 的 `AppTheme`/`ReadingTheme` 與 `assets/app_icon` 僅由閱讀排版使用，與 `custom_app_theme` 互為平行主題，改動需確認是否需雙向同步。
- **錯誤/日誌策略**：`lib/main.dart:59` `runZonedGuarded` + `ErrorWidget.builder` + `FlutterError.onError` → `lib/core/services/app_log_service.dart:8` `AppLog.put`（佇列 100 條、`_toastController`）→ `lib/core/services/crash_handler.dart:38` `recordFlutterError`/`recordError`/`_saveLog`。新增全域錯誤上報需在此三處同時接入。
- **背景任務**：`lib/main.dart:27` `runBackgroundTask`（可單測的注入式設計）→ `callbackDispatcher:42`（需保留 `@pragma('vm:entry-point')`）→ `lib/main.dart:195` `Workmanager().initialize`。Android 端無額外的 `AndroidManifest` WorkManager 配置，依賴 `workmanager` 插件自動註冊，改動需驗證原生初始化是否被 `try/catch` 吞掉（見 `_runPostFirstFrameStartupTasks` 的 `AppLog.e`）。
- **深連結/分享**：`lib/features/association/association_handler_service.dart:17` 單例入口 → `lib/features/association/handlers/association_base.dart:6` 生命週期 → `lib/features/association/handlers/uri_association_handler.dart:16`（`legado://`/`yuedu://` 映射表，新增 scheme 需在此擴充並補 `_typeDescription`）→ `lib/features/association/handlers/file_association_handler.dart:14`（`p.extension` 判斷與 `LocalBookFormats` 支援）→ `lib/features/association/handlers/association_dialog_helper.dart:16`（`showImportDialog` 的按鈕顯隱條件與 `SourceImportService`/`BookshelfExchangeService`/`ReplaceRuleDao` 呼叫）。`lib/main.dart:261` `_AssociationLifecycleHost` 是唯一掛載點，`dispose` 時 `dispose()` 取消訂閱。
- **關於/更新/崩潰日誌**：`lib/features/about/about_page.dart:51` 列表項 → `lib/features/about/update_check_runner.dart:10`（`runAutomatic`/`runManual` 與 `UpdateIgnoreStore`）→ `lib/features/about/update_dialog.dart:9`（`UpdateDialogResult.ignored/later` 與 `releasePageUrl`/`downloadUrl`）→ `lib/features/about/external_url_launcher.dart:6`（`launcher` 可注入以利測試）；`lib/features/about/crash_log_page.dart:8`（`readLogs`/`clearLogs`/`writeClipboard` 可注入）。
- **常量與配置**：`lib/core/constant/prefer_key.dart:2` 新增 key 後需同步 `lib/features/settings/settings_provider.dart:108` `_loadFromPrefs` 與 `lib/features/settings/provider/settings_base.dart` 的 `save`/`parseThemeMode`/`parseLocale`；`lib/core/constant/book_type.dart:24` 的 `allBookType`/`allBookTypeLocal` 被 `Search`/`BookSource` 等模組用於過濾，改動遮罩需全域搜尋；`lib/core/config/app_config.dart:4` 的靜態欄位需由 `SettingsProvider` 與 `ReaderSettingsMixin` 雙寫，新增欄位必須補同步點。

## Known Risks
- **Workmanager 雙重初始化與靜默失敗**：`main.dart:43` 的 `callbackDispatcher` 內再次 `configureDependencies`，而 `injection.dart:30` 每次呼叫皆 `registerSingleton`（非 `try` 或 `isRegistered` 保護），在背景 isolate 重入或 `_retryCriticalStartup` 的 `getIt.reset()` 後若有競爭，可能拋 `Assertion: Singleton already registered`；但 `main.dart:195` 的 `Workmanager().initialize` 與 `_startApp` 的頂層 `try/catch` 僅 `AppLog.e` 而不向用戶顯式提示，失敗時背景更新會靜默失效且難以察覺。
- **GetIt 與 Provider 的雙重事實來源**：`TTSService` 同時以 `getIt.registerSingleton`（`injection.dart:97`）與 `AppProviders.providers:21` `ChangeNotifierProvider.value(value: getIt<TTSService>())` 暴露，`SettingsProvider:200` 又直接 `TTSService().setRate/pitch/volume`（疑似單例工廠而非 `getIt` 實例），若 `TTSService` 的建構方式不一致，可能出現三個實例狀態分叉；`DownloadService` 僅由 `Provider` 創建而未註冊 `GetIt`，與其他 DAO 的定位不一致。
- **啟動時序與 Splash 的競態**：`MainPage._releaseSplashWhenShelfReady` 依賴 `BookshelfProvider.isLoading` 與手寫 `Timer` 監聽，若 `BookshelfProvider` 在 `initState` 後才因 `Provider` 延遲創建而改變 `isLoading`，或 `didChangeDependencies` 未觸發，`_splashShelfTimeout` 的 2 秒超時會錯誤顯示 `正在載入書架…` 遮罩；`_releaseNativeSplashOnce` 的 `widget.destinations != null` 提早返回（測試注入時）使 `flutter_native_splash` 的原生層未 `preserve` 卻 `remove`，可能在測試環境拋異常。
- **深連結的隱式契約與檔案系統假設**：`uri_association_handler.dart:44` 的 `type` 映射僅覆蓋 `bookSource`/`replaceRule`/`addToBookshelf`，其餘 `legado://import/*` 歸為 `auto` 並在 `AssociationDialogHelper` 顯示三個按鈕，呼叫端可能誤選；`file_association_handler.dart:46` 的 `getApplicationDocumentsDirectory/InkpageBooks` 複製後未清理分享快取，若連續分享同名檔案會因 `exists` 判斷直接跳過複製而沿用舊檔；`_handleSharedFile` 以 `first.containsKey('bookSourceUrl'/'pattern'/'themeName')` 判型，若 JSON 含多型混合則僅取首元素，可能誤判。
- **主題雙軌與持久化漂移**：`AppTheme`（`app_theme.dart:9` 讀寫 `readConfig.json`）與 `ThemeSettingsProvider`（`theme_settings_provider.dart:28` 讀寫 `SharedPreferences` 的 `theme_*_v1`）是兩套獨立的持久化與預設值（`paper200/ink600` vs `paper50/ink500`），`buildAppTheme` 與 `AppTheme.lightTheme/darkTheme` 的 `AppBar`/`Card` 參數也不一致，未來若只改其中一套會造成「設定頁改色但閱讀頁未跟隨」或反之的漂移。
- **PreferKey 的字串契約無遷移**：`PreferKey:2` 的 key 如 `process_text`/`tmp`/`cPrimary` 等為歷史遺留命名，`app_config.dart` 與 `SettingsProvider` 亦無版本化遷移框架（僅 `_migrateLegacySettings:206` 處理 `ttsSource` 單例），重新命名或刪除 key 會直接導致舊用戶資料孤兒化。
- **BookType 位元遮罩的隱式耦合**：`BookType:24` 的 `allBookType = text|image|webFile` 未包含 `local`/`archive`/`notShelf`，但 `archive=0x200` 的語義是「來自壓縮包」而非獨立顯示類型，`notShelf=0x400` 表示「未入架臨時書」，多處以 `(type & notShelf)==0` 判斷是否在架，若未來新增位元未更新 `allBookTypeLocal`，搜尋或篩選會靜默遺漏。
- **缺少自動化測試**：掃描範圍內無 `test/**/*app_shell*`、`test/**/*association*`、`test/**/*main_page*` 提交；僅 `CrashLogPage` 與 `launchExternalUrlWithFeedback` 對 `Clipboard`/`launcher` 做了可注入設計，但無對應測試覆蓋 `runBackgroundTask`、`UriAssociationHandler`、`_releaseSplashWhenShelfReady` 等關鍵分支。
- **平台配置的潛在不一致**：`AndroidManifest.xml:12` 的 `MainActivity` 未聲明 `legado://`/`yuedu://` 的 `<intent-filter>`（深連結依賴 `app_links` 插件在原生層動態註冊，若插件版本與 `singleTop`/`taskAffinity=""` 組合在部分 ROM 上可能收不到 `uriLinkStream`）；`ios/Runner/Info.plist` 未聲明對應 URL Schemes，iOS 深連結僅能透過 Universal Links 生效，現有 `legado://` 在 iOS 上無效但未在文件或 UI 提示。
- **遺留字體清理的不可逆**：`main.dart:202` `_cleanupLegacyCustomFontArtifacts` 每次啟動皆刪除 `documents/fonts/` 整個目錄，若用戶曾手動放置自訂字體（舊版功能），升級後會被靜默清空且無復原路徑。

## Boundaries
- **啟動必須先 DI 再 UI**：`main.dart:65` 的 `_startApp` 嚴格要求 `await configureDependencies()` 成功後才 `runApp`，且 `SettingsProvider`/`ThemeSettingsProvider` 的建構子同步讀取 `getIt<SharedPreferences>`（見 `settings_provider.dart:103` 與 `theme_settings_provider.dart:10`），因此 `SharedPreferences` 必須在 `injection.dart:100` 同步註冊為單例，任何將其改為非同步延遲註冊的變更會導致首幀 `getIt<SharedPreferences>` 拋 `NotRegistered`。
- **GetIt 單例不可重複註冊**：`injection.dart` 全程使用 `registerSingleton`/`registerLazySingleton` 而非 `registerSingletonIfAbsent`，呼叫前必須確保 `getIt.reset()` 已完成（如 `_retryCriticalStartup:145`），否則二次初始化會拋斷言；背景 isolate 的 `callbackDispatcher` 與主 isolate 各自擁有獨立 `GetIt` 實例，不可假設跨 isolate 共享狀態。
- **Splash 僅有一層原生層**：`flutter_native_splash.yaml:6` 明定「一段式啟動畫面，原生 splash 為主題色純色底，無 Flutter 端轉場圖」，`main.dart:68` `preserve` 與 `MainPage` 的 `FlutterNativeSplash.remove` 必須一對一配對，且 `android_12` 的 `@drawable/splash_icon` 為手寫資源，重跑 `flutter_native_splash:create` 會覆寫 `values-v31` 的 `LaunchTheme`，必須手動補回。
- **主題的雙軌不可互替**：`AppUiThemeColors`（`theme_customization.dart:9`）是 App 殼與導航的語意主題，`ReaderAreaThemeColors`（同檔 `113`）是閱讀正文與選單的主題，兩者序列化 key（`theme_app_*` vs `theme_reader_*`）與預設值完全分離，`ThemeSettingsProvider.areaMode` 的 `followSystem` 透過 `WidgetsBinding.instance.platformDispatcher.platformBrightness` 解析，App 殼的 `ThemeMode.system` 與閱讀區的 `AreaThemeMode.followSystem` 是兩個獨立開關，不可互相賦值。
- **PreferKey 與 AppPattern 為跨模組契約**：`PreferKey` 的字串值（如 `language`/`themeMode`/`userAgent`/`threadCount`/`replaceEnableDefault`）與 `AppPattern` 的正則（如 `jsPattern` 含 `<js>` 標籤、`imgPattern` 的寬鬆 `src` 匹配）被 `core/search`、`core/network`、`features/reader`、`features/settings` 等多模組直接引用，修改其字面量或大小寫敏感度屬於破壞性變更，需全域搜尋並提供遷移。
- **SourceType 與 BookType 的位元語義**：`SourceType:2` 的 `book=0` 是預設值，`audio=1`/`image=2`/`file=3` 對應後端書源的 Legado 映射，`rss` 已棄用僅作為 `file` 別名；`BookType:2` 的 `text=0x08` 與 `image=0x40` 互斥但可與 `local=0x100`/`archive=0x200`/`notShelf=0x400` 組合，判斷時必須用位元運算而非 `==`。
- **Association 的 UI 必須在 mounted 後**：`AssociationHandlerService.init:36` 的 `linkSubscription`/`sharedMediaSubscription` 回調與 `getInitialMedia`/`getInitialLink` 的 `then` 皆以 `context.mounted` 守衛，且 `showImportDialog` 內使用 `context` 與 `dialogContext` 兩個 `BuildContext`（前者用於後續 `ScaffoldMessenger`，後者用於 `Navigator.pop`），不可將兩者混用或在 `dispose` 後繼續持有。
- **平台權限與用途字串**：`AndroidManifest.xml:3` 僅聲明 `INTERNET`/`POST_NOTIFICATIONS`/`FOREGROUND_SERVICE`/`FOREGROUND_SERVICE_MEDIA_PLAYBACK`，未聲明 `READ_EXTERNAL_STORAGE`（分享檔案由 `receive_sharing_intent` 透過 `ContentProvider` 授權，無需額外權限）；`ios/Runner/Info.plist:30` 的 `NSPhotoLibraryUsageDescription` 僅用於封面選圖，`UIBackgroundModes audio` 為 TTS/朗讀保活所需，新增背景能力需同步更新兩平台配置。
- **發布與版本**：`pubspec.yaml:4` 的 `version: 0.2.146+160` 為 `versionName+buildNumber`，發布流程由 `AGENTS.md` 約束為 `flutter pub get` → `flutter analyze` → `flutter test` → `git push origin HEAD` → `git tag vX.Y.Z` → `git push origin vX.Y.Z`，且必須先推送分支再打 tag，tag 推送後由 `.github/workflows/android-release.yml` 觸發 Android 構建，改動發布相關配置需同時更新該 workflow 與 `flutter_launcher_icons`/`flutter_native_splash` 的資源管線。
