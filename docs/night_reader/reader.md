# 閱讀器 (reader)

## Responsibility
- 擁有 `夜讀` 唯一的正文閱讀會話與呈現管線：以 `lib/features/reader_v2/session/reader_v2_runtime.dart:29` 的 `ReaderV2Runtime` 為總控，串接 `lib/features/reader_v2/session/reader_v2_state_machine.dart:8` 的有限狀態機（`cold → loading → layingOut/restoring/switchingMode → ready → error`）、`lib/features/reader_v2/session/reader_v2_navigation_controller.dart:12` 的翻頁/跳章導航、`lib/features/reader_v2/session/reader_v2_resolver.dart:33` 的排版解析與快取、`lib/features/reader_v2/session/reader_v2_preload_scheduler.dart:22` 的背景預載，以及 `lib/features/reader_v2/hybrid/hybrid_reader_screen.dart:48` 方案 B 混合滾動視口；對上承接 `lib/features/reader_v2/screen/reader_v2_page.dart:30` 的頁面裝配，對下驅動 `lib/features/reader_v2/layout/reader_v2_layout_engine.dart:81` 與 `lib/features/reader_v2/hybrid/pump/layout_pump.dart:13` 兩套排版引擎。
- 擁有章節內容的獲取與變換：`lib/features/reader_v2/chapter/reader_v2_chapter_repository.dart:29` 封裝 `ChapterDao`/`ReaderChapterContentDao`/`BookSourceService` + `ReaderChapterContentStorage` 的網路/快取/本地書三路徑，`lib/features/reader_v2/chapter/reader_v2_content_transformer.dart:1` 執行替換規則與 `lib/core/engine/reader/chinese_text_converter.dart:3` 的 `ChineseTextConverter.convert`（`convertType 0/1/2` 委派 `ChineseUtils.s2t/t2s`），`lib/features/reader_v2/chapter/reader_v2_content.dart:5` 與 `lib/features/reader_v2/chapter/reader_v2_processed_chapter.dart:1` 定義正規化與雜湊契約。
- 擁有閱讀位置的單一真相：`lib/features/reader_v2/session/reader_v2_location.dart:1` 的 `ReaderV2Location(chapterIndex, charOffset, visualOffsetPx)`（`visualOffsetPx` 限幅 `[-120, 120]`，`anchorOffsetInViewport` 為 `viewportHeight*0.2 clamp [24,120]`），`lib/features/reader_v2/session/reader_v2_viewport_bridge.dart:1` 的 `ReaderV2ViewportBridge` 負責 viewport 的 `captureVisibleLocation` / `viewportRestore` 註冊與 `ReaderV2ProgressController` 的防抖落盤，`lib/features/reader_v2/session/reader_v2_progress_controller.dart:10` 以 400ms debounce + `_activeFlush` 串列化寫入 `BookDao.updateProgress` 與 `book.readerAnchorJson`。
- 擁有閱讀輔助能力的會話級協調：`lib/features/reader_v2/features/auto_page/reader_v2_auto_page_controller.dart:1` 的自動翻頁（經 `ReaderV2ViewportController` 七閉包）、`lib/features/reader_v2/features/bookmark/reader_v2_bookmark_controller.dart:1` 的書籤、`lib/features/reader_v2/features/tts/reader_v2_tts_controller.dart:1` + `reader_v2_tts_highlight.dart:1` 的朗讀與字級高亮跟隨、`lib/features/reader_v2/features/replace_rule/reader_v2_replace_rule_sheet.dart:1` 的替換規則編輯、`lib/features/reader_v2/features/settings/reader_v2_settings_controller.dart:10` 的字體/字距/行高/縮排/主題等排版設定，以及 `lib/features/reader_v2/features/menu/reader_v2_menu_controller.dart:3` 的選單狀態。
- 擁有 viewport 的跨層抽象：`lib/features/reader_v2/viewport/reader_v2_viewport_controller.dart:12` 的七閉包 `scrollBy/continuousScrollBy/animateBy/moveToNextPage/moveToPrevPage/settleScroll/ensureCharRangeVisible`（前六個經 `HybridReaderScreen._HybridCommandQueue` FIFO，`settleScroll` 直達），`lib/features/reader_v2/viewport/reader_v2_pointer_tap_layer.dart:1` 的點擊分區，`lib/features/reader_v2/hybrid/view/hybrid_scroll_view.dart:1` 與 `lib/features/reader_v2/hybrid/view/hybrid_block_sliver.dart:1` 的自定義 `RenderSliver` 管線。
- 擁有混合架構的增量排版與度量管線：`lib/features/reader_v2/hybrid/measure/document_index.dart:32` 的 `DocumentIndex`（雙 Fenwick 索引、以 `BlockKey` 為錨的增量 admit）、`lib/features/reader_v2/hybrid/measure/measurement_store.dart:6` 的 `MeasurementStore`（`MeasurementNamespace(epoch, fingerprint)` → `BlockMetrics`）、`lib/features/reader_v2/hybrid/measure/metrics_disk_cache.dart:1` 的磁碟度量快取、`lib/features/reader_v2/hybrid/paragraph/paragraph_cache.dart:1` 的 `ParagraphCache`（LRU + pin）、`lib/features/reader_v2/hybrid/pump/layout_pump.dart:13` 的 `LayoutPump`（`BudgetGovernor` + `LayoutCostModel` 預算制）、`lib/features/reader_v2/hybrid/text/text_preprocessor.dart:1` 的段落切塊、`lib/features/reader_v2/hybrid/anchor/anchor_manager.dart:4` 的 `HybridAnchor` 換算。

## Scope
- **代表性資料夾與檔案**：
  - 頁面與裝配：`lib/features/reader_v2/screen/reader_v2_page.dart`（`ReaderV2Page(book, openTarget, initialChapters)` + `_ReaderV2PageState` 組裝 `ReaderV2PageShell`/`HybridReaderScreen`/`ReaderV2ChaptersDrawer`）、`lib/features/reader_v2/screen/reader_v2_page_shell.dart:1`（`Scaffold` + `AppBar` + 進度列 + 選單插槽，其 `controlsVisible/showReadTitleAddition` 來自 `Menu/Settings`）、`lib/features/reader_v2/screen/reader_v2_chapters_drawer.dart:1`（章節清單抽屜，`onChapterTap → jumpToChapter`）、`lib/features/reader_v2/screen/reader_v2_controller_host.dart:23`（`ReaderV2ControllerHost` 持有 `Settings/Menu/Viewport/TTS/AutoPage/Bookmark/Runtime/Dependencies/BookStorageService` 並在 `ensureRuntime/syncRuntimeConfiguration` 中以 `LayoutBuilder` 的 `Size` + `ReaderV2Style` 生成 `ReaderV2LayoutSpec`）、`lib/features/reader_v2/screen/dependencies/reader_v2_dependencies.dart:1`（`ReaderV2Dependencies` 封裝 `BookDao/ChapterDao/BookSourceDao/BookmarkDao/ReplaceRuleDao/ReaderChapterContentDao` 的 `GetIt` 解析與 `createChapterRepository` 工廠）。
  - 會話核心：`lib/features/reader_v2/session/reader_v2_runtime.dart`（`ChangeNotifier`，`openBook/applyPresentation/reloadContentPreservingLocation/jumpToChapter/jumpToLocation/restoreFromLocation`，`hybridViewportActive` 分流，`stateMachine/resolver/preloadScheduler/viewportBridge/navigation/progressController`）、`lib/features/reader_v2/session/reader_v2_state.dart:6`（`ReaderV2Phase{cold,loading,layingOut,restoring,ready,switchingMode,error}` + `ReaderV2State(phase, committedLocation, visibleLocation, layoutSpec, layoutGeneration, pageWindow, errorMessage)`）、`lib/features/reader_v2/session/reader_v2_state_machine.dart`（`beginOpen/beginJump/beginRestore/beginPresentation/beginContentReload` 產生 `ReaderV2OperationToken(id,kind,layoutGeneration)` 並以 `isCurrent` 判定過期，`completeReady/fail/updateVisibleLocation/commitLocation`）、`lib/features/reader_v2/session/reader_v2_location.dart`（`visualOffsetPx` 限幅與 `normalized/chapterLength` 鉗制，`toJson/fromJson`）、`lib/features/reader_v2/session/reader_v2_open_target.dart:1`（`ReaderV2OpenTarget{location, intent}`，`resume/chapterStart`）、`lib/features/reader_v2/session/reader_v2_operation_token.dart:1`（`ReaderV2OperationKind{open,jump,restore,presentation,contentReload}`）、`lib/features/reader_v2/session/reader_v2_page_window.dart:1`（`ReaderV2PageWindow(prev, current, next)` 三頁視窗）、`lib/features/reader_v2/session/reader_v2_resolver.dart`（`layoutSpec` + `_layouts(≤50)/_cursors/_inFlight/_layoutErrors`，`ensureLayout/ensureLayoutAtLeast/continueLayoutStep/nextPage/prevPage/placeholderPageFor`，`_maxStepExtentPx=3000`，`StaleLayoutGeneration`）、`lib/features/reader_v2/session/reader_v2_navigation_controller.dart`（`moveToNextPage/moveToPrevPage` 的三視窗滑動與 `nextPageOrPlaceholder` 佔位，`jumpToLocation/restoreFromLocation` 的 `_windowAroundPage`，`refreshNeighbors` 與 pending-neighbor 自動接續）、`lib/features/reader_v2/session/reader_v2_preload_scheduler.dart`（`contentQueue/layoutQueue` 雙佇列，`maxConcurrent=1`，`beginInteractive/endInteractive` 暫停排版，`scheduleOpen/scheduleJump/scheduleScrollSettled/scheduleDirectional/scheduleAround`，`_clearQueued/bumpGeneration`）、`lib/features/reader_v2/session/reader_v2_viewport_bridge.dart`（`registerVisibleLocationCapture/registerViewportRestore` 的 owner 語意，`captureVisibleLocation/saveProgress/saveJumpAfterSettled/flushProgress` 的 debounce 與 token 鑑別）、`lib/features/reader_v2/session/reader_v2_progress_controller.dart`（`schedule/saveImmediately/flush`，`book.readerAnchorJson` 與 `updateProgress`）、`lib/features/reader_v2/session/reader_v2_read_time_controller.dart:1` + `reader_v2_read_time_scope.dart:1`（閱讀時長統計）、`lib/features/reader_v2/session/reader_v2_chapter_view.dart:1`（`ReaderV2ChapterView` 包 `ReaderV2ChapterLayout`）、`lib/features/reader_v2/session/reader_v2_performance_metrics.dart:1`（`FrameTiming` 採樣與 profiling signal）。
  - 章節與內容：`lib/features/reader_v2/chapter/reader_v2_chapter_repository.dart`（`_contentCache LRU 20` + `_contentInFlight` 去重，`ensureChapters/cachedContent/loadContent/preloadContent/clearContentCache`，`_loadViaV2ContentPipeline` 經 `ReaderChapterContentStorage.withMaterializer` + `ReaderV2ContentTransformer`）、`lib/features/reader_v2/chapter/reader_v2_content.dart`（`fromRaw/normalizeRawText` 以 `\n{3,}→\n\n` 正規化，`plainText/displayText/contentHash(sha1)`）、`lib/features/reader_v2/chapter/reader_v2_content_transformer.dart`（替換規則 + `ChineseTextConverter`）、`lib/features/reader_v2/chapter/reader_v2_processed_chapter.dart`（`ReaderV2ProcessedChapter`）。
  - 排版與渲染：`lib/features/reader_v2/layout/reader_v2_layout_spec.dart:44`（`ReaderV2LayoutSpec(viewportSize, contentWidth, contentHeight, style, cellWidth)`，`layoutSignature = Object.hash(...)`，`anchorOffsetInViewport = h*0.2 clamp [24,120]`，`fromViewport` 的 em-grid 鎖寬 `cellWidth * cells + 0.05`）、`lib/features/reader_v2/layout/reader_v2_layout.dart:1`（`ReaderV2ChapterLayout(lines, pages, contentHeight, isComplete)` + `ReaderV2TextLine/ReaderV2PageSlice`）、`lib/features/reader_v2/layout/reader_v2_layout_engine.dart:81`（`layout/layoutStep`，`TextPainter` 雙實例複用，`lineStartForbidden/lineEndForbidden` 中文避頭尾，英文單字防切斷，`_layoutYieldBudget` 半幀預算與 `_yieldSlice` 幀感知讓出，`_fitLineChars/_measureLineWidth` 二分搜尋）、`lib/features/reader_v2/layout/reader_v2_layout_constants.dart:1`（`kReaderContentTopSpacing/kReaderContentTopSafeAreaFactor` 等）、`lib/features/reader_v2/layout/reader_v2_style.dart:1`（`ReaderV2Style(fontSize, lineHeight, letterSpacing, paragraphSpacing, padding, bold, textIndent, lastLineSpacingCompensation)`）、`lib/features/reader_v2/layout/reader_v2_typography.dart:1`（`kReaderV2CjkFontFeatures/kReaderV2CjkTypographyFeatureSignature`）、`lib/features/reader_v2/render/reader_v2_render_page.dart:1`（`ReaderV2RenderPage(chapterIndex, pageIndex, pageCount, lines, contentHeight, viewportHeight, startCharOffset, endCharOffset, isLoading/errorMessage/isPlaceholder)` + `ReaderV2RenderLine`）、`lib/features/reader_v2/render/reader_v2_line_box.dart:1`（行盒幾何）、`lib/features/reader_v2/render/reader_v2_text_adapter.dart:1`（文字適配）。
  - 混合視口：`lib/features/reader_v2/hybrid/hybrid_reader_screen.dart`（`HybridReaderScreen(runtime, style, viewportController, ttsHighlight, progressListenable, bookUrl, preprocessor)`，`_HybridCommandQueue`、`DocumentIndex + AnchorManager + BudgetGovernor + ParagraphCache + LayoutPump + AdmissionController + HybridChapterRepository + MeasurementStore/MetricsDiskCache + Telemetry`）、`lib/features/reader_v2/hybrid/core/hybrid_types.dart`（`BlockKey/LayoutEpoch/StyleFingerprint/MeasurementNamespace/BlockMetrics/HybridTextRange/BlockRange/ChapterBlock/ChapterBlocks/HybridAnchor/ChapterText/ChapterEvent/HybridBlockTextStyle/LayoutTask/BlockReady/HybridLineBox`）、`lib/features/reader_v2/hybrid/core/hybrid_contracts.dart:1`（`HybridMeasurementStore/HybridDocumentIndex/HybridChapterTextRepository/HybridTextPreprocessor/HybridParagraphCache/HybridLayoutPump/HybridProgressCalculator`）、`lib/features/reader_v2/hybrid/anchor/anchor_manager.dart`（`captureFromLocation/beginRestore/completeRestore/bumpEpoch`，`anchorOffsetInViewport` 復用 `ReaderV2LayoutSpec` 公式）、`lib/features/reader_v2/hybrid/measure/document_index.dart`（`_beforeCenter/_centerAndAfter` + 雙 `_FenwickTree`，`reset/admit/topOf/bottomOf/blockAtOffset/keysInRange/chapterRange`，`_revision Listenable` 直驅 sliver relayout）、`lib/features/reader_v2/hybrid/measure/measurement_store.dart`（`Map<MeasurementNamespace, Map<BlockKey, BlockMetrics>>`）、`lib/features/reader_v2/hybrid/measure/metrics_disk_cache.dart`（`path_provider` 路徑，`StyleFingerprint.stableKey` jsonEncode 跨程序穩定鍵）、`lib/features/reader_v2/hybrid/paragraph/paragraph_cache.dart`（`Map<BlockKey, ui.Paragraph>` + LRU + `pinRange/unpinAll`，`capacity` 預設 512）、`lib/features/reader_v2/hybrid/pump/layout_pump.dart`（`submit/pumpPending/onScrollStateChanged`，`measureCellWidth` 以 `「一一」` 兩字 x 差量測 `cellWidth` 並快取，`_buildParagraph` 兩段式 `justify + 末行補償`，`BudgetGovernor` 幀預算與 `LayoutCostModel` 字數→耗時模型）、`lib/features/reader_v2/hybrid/pump/budget_governor.dart:1`（`frameBudgetMicros/recordFrameTimings/recordPumpWork/hasLeadDeficit`）、`lib/features/reader_v2/hybrid/pump/layout_cost_model.dart:1`（`predict/layoutPassesFor/mayCompensateLastLine/maxCharsFor`）、`lib/features/reader_v2/hybrid/text/hybrid_chapter_repository.dart:1`（`HybridChapterRepository(repository)` + `ChapterEvent` 流）、`lib/features/reader_v2/hybrid/text/text_preprocessor.dart:1`（`process(ChapterText, maxBlockChars)` 以 `maxCharsForBudget(governor.ballisticSliceBudget)` 切塊，`useIsolate` 旗標）、`lib/features/reader_v2/hybrid/view/admission_controller.dart:1`（`AdmissionController(documentIndex, pump.completed)`，`registerChapter/offer/reset/activateViewport/updateLead/atForwardBookBoundary`，`guaranteedWindow/backwardGuaranteedWindow/forwardLeadPx/latestForwardLead`）、`lib/features/reader_v2/hybrid/view/hybrid_scroll_view.dart:1`（`CustomScrollView` + `HybridScrollPhysics(admission)` 單一穩定 `ScrollPhysics`，`HybridBlockSliver` 前後兩段）、`lib/features/reader_v2/hybrid/view/hybrid_block_sliver.dart:1`（`RenderHybridBlockSliver` 覆寫 `computeMaxScrollOffset/estimateMaxScrollOffset` 以 Fenwick `O(log n)` 替代框架 `O(n)` 掃描）、`lib/features/reader_v2/hybrid/view/cached_block_widget.dart:1`（`CachedBlockWidget(paragraph, textColor)`）、`lib/features/reader_v2/hybrid/overlay/selection_service.dart:1`（選詞服務）、`lib/features/reader_v2/hybrid/overlay/tts_highlight_overlay.dart:1`（`TtsHighlightOverlay` 以 `getBoxesForRange` 轉 `HybridLineBox`）、`lib/features/reader_v2/hybrid/progress/hybrid_progress.dart:1`（`HybridProgress(documentIndex, chapterCount).progressForOffset(offset) → HybridProgressSnapshot`）、`lib/features/reader_v2/hybrid/telemetry/hybrid_telemetry.dart:1` + `hybrid_debug_overlay.dart:1`（幀採樣與除錯疊層）。
  - 互動與用例：`lib/features/reader_v2/viewport/reader_v2_viewport_controller.dart`（七閉包契約）、`lib/features/reader_v2/viewport/reader_v2_pointer_tap_layer.dart:1`（九宮格 `ReaderV2TapAction.fromCode` 映射）、`lib/features/reader_v2/use_cases/reader_v2_page_coordinator.dart:13`（`handleTap/jumpRelativeChapter/jumpToChapter/previewChapterPercent/commitChapterPercent/jumpToCurrentChapterPercent/toggleAutoPage/toggleBookmark/maybeFollowTtsHighlight/openReplaceRule`，`scrubPreviewDebounce 180ms`）、`lib/features/reader_v2/use_cases/coordinators/reader_v2_chapter_navigation_resolver.dart:1`（`resolveRelativeTarget` 邊界守衛）、`lib/features/reader_v2/use_cases/coordinators/reader_v2_display_coordinator.dart:1`（顯示協調）、`lib/features/reader_v2/use_cases/coordinators/reader_v2_page_exit_coordinator.dart:1`（離場前 `persistExitProgress/discardUnkeptBookStorage/shouldPromptAddToBookshelfOnExit`）。
  - 功能切片：`lib/features/reader_v2/features/menu/reader_v2_menu_controller.dart`（`controlsVisible/isScrubbing/scrubPercent`）、`lib/features/reader_v2/features/menu/reader_v2_bottom_menu.dart:1` + `reader_v2_top_menu.dart:1` + `reader_v2_tap_action.dart:1` + `reader_v2_menu_palette.dart:1`（九宮格 `clickActions[9]`）、`lib/features/reader_v2/features/settings/reader_v2_settings_controller.dart`（`fontSize/lineHeight/paragraphSpacing/letterSpacing/textIndent/lastLineSpacingCompensation/themeIndex/chineseConvert/autoPageSpeed/clickActions`，`readStyleFor/setTypography`，`contentSettingsGeneration` 觸發 `reloadContentPreservingLocation`）、`lib/features/reader_v2/features/settings/reader_v2_prefs_repository.dart:1`（`SharedPreferences` 鍵 `reader_*_v2` 與 `cachedSnapshot`）、`lib/features/reader_v2/features/settings/reader_v2_settings_sheets.dart:1` + `reader_v2_setting_components.dart:1`（設定面板）、`lib/features/reader_v2/features/auto_page/reader_v2_auto_page_controller.dart`（`viewportExtent/autoPageSpeed` 閉包，`toggle/isRunning/takeUserNotice/refreshConfiguration`）、`lib/features/reader_v2/features/bookmark/reader_v2_bookmark_controller.dart`（`addVisibleLocationBookmark`）、`lib/features/reader_v2/features/tts/reader_v2_tts_controller.dart`（`TTSService` 包裝，`currentHighlight/loadSettings/toggle`）、`lib/features/reader_v2/features/tts/reader_v2_tts_highlight.dart`（`ReaderV2TtsHighlight(chapterIndex, highlightStart, highlightEnd, isValid)`）、`lib/features/reader_v2/features/tts/reader_v2_tts_sheet.dart:1`（TTS 操作面板）、`lib/features/reader_v2/features/replace_rule/reader_v2_replace_rule_sheet.dart` + `reader_v2_replace_rule_editor_sheet.dart:1` + `reader_v2_replace_rule_page.dart:1`（`ReplaceRuleDao` CRUD 與 `onReload` 回調）。
  - 轉換與工具：`lib/core/engine/reader/chinese_text_converter.dart`（`convert(text, convertType)`）、`lib/features/reader_v2/features/replace_rule/*` 的規則管線與 `ChineseTextConverter` 的銜接點 `ReaderV2ContentTransformer`。
- **公開 API / 進入點**：`ReaderV2Page(book, openTarget, initialChapters)`；`ReaderV2ControllerHost.ensureRuntime/syncRuntimeConfiguration/specFromStyle/flushProgress`；`ReaderV2Runtime.openBook/applyPresentation/reloadContentPreservingLocation/jumpToChapter/jumpToLocation/restoreFromLocation/moveToNextPage/moveToPrevPage/saveProgress/flushProgress/registerHybridViewport/registerVisibleLocationCapture/registerViewportRestore`；`ReaderV2Resolver.ensureLayout/ensureLayoutAtLeast/continueLayoutStep/cachedLayout/placeholderPageFor/nextPage/prevPage/nextPageOrPlaceholder/prevPageOrPlaceholder/updateLayoutSpec/clearCachedLayouts/retainLayoutsFor`；`ReaderV2NavigationController.moveToNextPage/moveToPrevPage/jumpToLocation/jumpToChapter/restoreFromLocation/refreshNeighbors/preloadDirectionalForVelocity`；`ReaderV2ViewportController` 七閉包；`HybridReaderScreen` 的 capture/restore 與七命令；`ChineseTextConverter.convert`。
- **命令與測試**：`flutter analyze` / `flutter test`（`AGENTS.md` 發布流程）；`test/features/reader_v2/reader_v2_layout_engine_test.dart`、`reader_v2_content_transformer_test.dart`、`reader_v2_resolver_test.dart` + `*_stress_test.dart`、`reader_v2_state_machine_test.dart`、`reader_v2_navigation_viewport_bridge_test.dart`、`reader_v2_page_shell_test.dart` 等；`test/features/reader_v2/hybrid/hybrid_core_test.dart`、`hybrid_measure_test.dart`、`hybrid_pump_test.dart`、`hybrid_scroll_behavior_test.dart`、`hybrid_text_test.dart`、`text_preprocessor_test.dart` 等；`test/core/engine/reader/*`（若有 `chinese_text_converter_test.dart`）。

## Dependencies & Impact
- **上游依賴**：
  - 套件：`flutter`（`TextPainter`/`ParagraphBuilder`/`RenderSliver`/`Scrollable`/`CustomScrollView`）、`provider`（頁面外層仍由 `AppProviders` 注入 `ThemeSettingsProvider` 等，`ReaderV2SettingsController` 內部為 `ChangeNotifier` 不經 `Provider`）、`get_it`（`ReaderV2Dependencies` 以 `getIt<BookDao/ChapterDao/...>` 解析）、`shared_preferences`（`ReaderV2PrefsRepository` 的 `reader_*_v2` 鍵）、`drift`（`AppDatabase` + `BookDao/ChapterDao/BookmarkDao/ReplaceRuleDao/ReaderChapterContentDao`）、`path_provider`（`MetricsDiskCache` 與 `BookHelp` 路徑）、`crypto`（`ReaderV2Content.contentHash` 的 `sha1`）、`flutter/foundation`（`kDebugMode`、`ChangeNotifier`）。
  - 內部：`core/models/book.dart:1`/`chapter.dart:1`/`book_source.dart:12`/`replace_rule.dart:1`（`Book{bookUrl, origin, chapterIndex, charOffset, visualOffsetPx, durChapterTitle, readerAnchorJson}` 與 `BookChapter{index, url, title, content}`）、`core/services/book_source_service.dart:1`（`getChapterList/getContent` 供 `ReaderV2ChapterRepository` 與 `ReaderChapterContentStorage`）、`core/services/reader_chapter_content_store.dart:1` + `reader_chapter_content_storage.dart:1`（三階段 `ReaderChapterContentStorage.read`：快取→網路→DB 回填）、`core/services/book_storage_service.dart:1`（`discardBook` 供離場未入架清理）、`core/services/source_switch_service.dart:1`（`ReaderV2Page` 換源 `ChangeSourceSheet` 用，`resolveSwitch/persistSwitch`）、`core/services/app_log_service.dart:7`（`HybridReaderScreen._logTelemetrySessionSummary` 寫 `AppLog.i`）、`core/engine/reader/chinese_text_converter.dart:3`（簡繁轉換）、`core/utils/network_utils.dart:1`（`getAbsoluteURL` 供 `BookListParser` 類比）、`core/config/app_config.dart:4`（`readerV2ContentJustify/readerLastLineSpacingCompensation` 供 `HybridReaderScreen._buildContent` 與 `StyleFingerprint`）、`shared/theme/app_theme.dart:9`（`AppTheme.readingThemes` 供 `ReaderV2SettingsController._themeAt`）、`features/settings/theme_settings_provider.dart:9`（`resolveReaderTheme/resolveAreaDarkMode/saveAreaMode` 的市集主題解析）、`features/search`/`features/book_detail` 的 `ChangeSourceSheet` 與 `SourceSwitchService`、路由 `shared/navigation/book_open_route.dart:14`（`BookOpenRoute` 換源後 `pushReplacement`）。
- **下游影響**：
  - `features/welcome/main_page.dart:38` 的 `BookOpenRoute(book, openTarget, initialChapters)` 是唯一入口，`initialChapters` 的空/非空決定 `ReaderV2ChapterRepository.ensureChapters` 是否走網路；改動 `ReaderV2OpenTarget` 的 `intent` 會影響 `ReaderV2ControllerHost._initialLocationFor` 的 `anchorOffsetInViewport` 對齊與 `ReaderV2NavigationController._isTopAlignedChapterStart` 判定。
  - `features/bookshelf` 的 `BookDao.updateProgress` 被 `ReaderV2ProgressController._write` 寫入 `chapterIndex/charOffset/visualOffsetPx/readerAnchorJson` 四欄，`readerAnchorJson` 的 `ReaderV2Location.toJson` 形狀為 `{chapterIndex, charOffset, visualOffsetPx}`，改動任一欄需同步遷移舊資料與 `ReaderV2Location.fromJson` 的寬容解析。
  - `features/bookmark` 的 `BookmarkDao` 由 `ReaderV2BookmarkController` 封裝，`ReaderV2Location` 的 `charOffset` 直通書籤，改動 `displayText` 的 `title + \n\n + plainText` 拼接（`ReaderV2Content:44`）會使既有書籤的 `charOffset` 指向錯誤文本。
  - 全域 `AppConfig.readerV2ContentJustify` 同時影響 `HybridReaderScreen` 的 `HybridBlockTextStyle.justify` 與 `StyleFingerprint.justify`，`ReaderV2LayoutSpec.layoutSignature` 也包含 `kReaderV2CjkTypographyFeatureSignature`，三者任一變更皆使 `MeasurementStore` 與 `DocumentIndex` 的舊度量失效（`HybridReaderScreen._handleEpochRebuild` 清 `namespace` 並重置 `DocumentIndex`），需評估首開重排成本。

## Key Flows
- **開書與首幀恢復**：`ReaderV2Page:66` `initState` 建 `ReaderV2ControllerHost` + `ReaderV2PageCoordinator` → `LayoutBuilder:204` 每幀以 `constraints` + `MediaQuery.paddingOf` 取 `Size` 與 `style = settings.readStyleFor(mediaPadding)`，`host.ensureRuntime(size, style)` 以 `ReaderV2LayoutSpec.fromViewport(size, cellWidth, style)` 生成 `initialLayoutSpec`（`cellWidth = LayoutPump.measureCellWidth(fontSize, letterSpacing, bold)`，失敗為 `null` 不鎖寬）並建 `ReaderV2Runtime(book, repository, layoutEngine, progressController, initialLayoutSpec, initialLocation)`，`_initialLocationFor` 若 `openTarget.intent==chapterStart` 則以 `spec.anchorOffsetInViewport` 對齊，否則沿用 `Book{chapterIndex, charOffset, visualOffsetPx}` → `WidgetsBinding.addPostFrameCallback(_openRuntimeAfterFirstFrame)` 內 `runtime.openBook()`：`stateMachine.beginOpen → repository.ensureChapters()`（空則 `sourceDao.getByUrl(origin)` + `BookSourceService.getChapterList` + `insertChapters`）→ `initialLocation.normalized(chapterCount)` → 若 `hybridViewportActive` 則走 `HybridReaderScreen` 的 `_positionHybridViewport`（見下），否則若 `viewportBridge.viewportRestore != null` 先 `navigation.restoreFromLocation(location)` 嘗試以 `DocumentIndex` 既有度量直接定位，失敗再 `beginOpen` 重來並 `navigation.jumpToLocation(location, immediateSave:false)` 排出 `PageWindow(prev,current,next)` 並 `completeReady`，末 `preloadScheduler.scheduleOpen(center)` 以 `radius 1` 預載鄰章內容與排版。
- **樣式/尺寸變更的呈現切換**：`ReaderV2ControllerHost.syncRuntimeConfiguration:132` 每幀比對 `spec.layoutSignature`，`needsLayout` 時 `addPostFrameCallback → runtime.applyPresentation(spec)`：先 `navigation.clearPendingNeighborAdvance()` + `captureVisibleLocation()` 快照回退位置 → 若 `hybridViewportActive` 則 `stateMachine.beginPresentation(spec, generation+1)` + `_positionHybridViewport`；否則 `preloadScheduler.bumpGeneration()`（清 `queuedLayoutKeys` 並完成對應 waiter）+ `resolver.updateLayoutSpec(spec)`（清 `_layouts/_cursors/_layoutErrors` 並標記 `invalidatedInFlightTaskIds`）+ `navigation.jumpToLocation(location, immediateSave:false)`，待仍為當前 `layoutSignature` 且非 `error` 再 `completeReady`。`chineseConvert` 變更時（`settings.contentSettingsGeneration` 遞增）另走 `runtime.reloadContentPreservingLocation()`：`clearContentCache/clearCachedLayouts` → `jumpToLocation`（hybrid 分支同為 `_positionHybridViewport`）。
- **增量排版與快取**：`ReaderV2Resolver.ensureLayoutAtLeast:137` 以 `minExtentPx` 為界迴圈 `_stepOnce`——`_stepOnce:178` 以 `'$chapterIndex|${spec.layoutSignature}|$cacheGeneration'` 去重 `_inFlight`，`stepTarget = min(remainingNeeded, 3000)`，`repository.loadContent` → 取 `existingView.linesSoFar` 與 `cursor` 續跑 `layoutEngine.layoutStep(content, spec, linesSoFar, cursor, minNewExtentPx: stepTarget)`（`ReaderV2LayoutEngine:198` 內以 `halfFrameUs = 1e6/refreshRate/2` 為 `_layoutYieldBudget`，段落邊界以 `SchedulerBinding.endOfFrame` 幀感知讓出），產 `ReaderV2ChapterView(layout, title)` 寫 `_layouts(≤50 LRU)` 並 `onChapterProgressed` 通知；`ReaderV2PreloadScheduler._pumpLayout` 以 `continueLayoutStep` 輪詢多章，每章 `isComplete==false` 時 `requeue` 到尾端避免單一長章獨占。`HybridReaderScreen` 的同構管線：`TextPreprocessor.process(text, maxBlockChars)` 以 `governor.ballisticSliceBudget` 切 `ChapterBlocks` → `LayoutPump.submit(LayoutTask(block, epoch, fingerprint, textStyle, contentWidth, cellWidth, indentChars, trailingSpacing, priority, direction))` → `pumpPending` 以 `governor.frameBudgetMicros(state)` 與 `LayoutCostModel.predict` 打包，`_buildParagraph` 以 `ui.ParagraphBuilder` 實排並寫 `ParagraphCache + MeasurementStore`，經 `AdmissionController.offer` 匯入 `DocumentIndex`。
- **Hybrid 錨點定位與捕獲**：`HybridReaderScreen._restoreCore:469`（`openBook/applyPresentation/reloadContentPreservingLocation/ensureCharRangeVisible` 共用）以 `++_restoreTicket` 取 `still()` 世代守衛 → `pump.onScrollStateChanged(rebuilding)` → `_ensureChapterBlocks(chapterIndex)`（`HybridChapterRepository.load` + `preprocessor.process` + `_warmDiskMetricsForChapter` 回填 `MeasurementStore`）→ `location.normalized(chapterLength)` → `anchorManager.captureFromLocation → HybridAnchor.fromLocation(blockForCharOffset)` → `documentIndex.reset(centerKey: anchor.blockKey)` + `admission.reset/attach(register loaded _blocks)` → `_paragraphCache.unpinAll/pinKeys([anchor.blockKey])` + `_restorePinning=true` + `_enqueued.clear()` → `_ensureWindowTasks(anchorKey)` 以 `[0,1,-1,2,-2]` 在 `_windowCenter` 周圍對每章按 `centerKey` 前後向交錯以 `direct-admit`（`measurementStore.get + paragraphCache.containsFresh` 且本側未斷檔）或 `pump.submit` 投放 → `_pumpUntilAnchorReady` 迴圈 `pumpPending` 直至 `measurementStore.get(anchor) != null && paragraphCache.contains(anchor)` 且 `beforeExtent/afterExtent` 覆蓋首屏 `target ± guaranteedWindow` → `_offsetForAnchor = documentIndex.topOf(anchor.blockKey) + lineTopForChar(indent+charOffset) - anchorOffsetInViewport + visualOffsetPx` → `_applyScrollOffset(target)`（`ScrollController.position.jumpTo` 或 `pendingScrollOffset`）→ `admission.activateViewport(visibleTop/Bottom)` → `pump.onScrollStateChanged(idle/dragging)`。`_captureVisibleLocation:395` 反向以 `effectiveScrollOffset + anchorOffsetInViewport → worldY → documentIndex.hitTest(worldY)` 取 `BlockKey`，`paragraph.getPositionForOffset(0, lineTop)` 與 `_textBoxTopForOffset` 換算 `visualOffsetPx` 與 `charOffset`（含 `indentChars` 扣減與 `charRange` 鉗制），供 `ViewportBridge.captureVisibleLocation` 與運動中 `scheduleMotionCapture` 呼叫。
- **滾動、翻頁與進度落盤**：`HybridReaderScreen._handleScrollNotification:914` 於 `ScrollStart(dragDetails!=null)→dragging + pump.dragging`、`ScrollEnd(wasUser)→idle + _handleScrollSettled`；`_scheduleMotionCapture:940` 於每幀 `captureAndReport(notify:false)`（`widget.runtime.captureVisibleLocation(notifyIfChanged:false)` 避免連鎖 `ReaderV2Page.setState`），`admission.updateViewport + updateParagraphPins + publishProgress + updateLeadTelemetry`，跨章時 `_shiftWindow` 改 `chapterRepo.setPrefetchCenter` 並 `ensureWindowTasks/schedulePump`；`_handleScrollSettled:980` 以 `captureAndReport(notify:true)` + `runtime.saveProgress(immediate:true)` 同步落盤，`_publishProgress` 以 `HybridProgress.progressForOffset(worldY)` 經 `progressListenable` 窄通道驅動 `ReaderV2PageShell` 的章段與全書百分比；`_movePageNow:1143` 以 `viewportHeight - overlap(≥24, fontSize*lineHeight)` 為步長調 `animateBy`，到書頭/尾時 `emitUserNotice`。非 hybrid 的分頁路徑由 `ReaderV2NavigationController.moveToNextPage/moveToPrevPage:39` 以 `PageWindow` 的 `next/prev` 三視窗滑動，`isPlaceholder` 時以 `isLoading` 決定 pending-neighbor 或錯誤提示並 `scheduleLayout(target, priority:true)` 觸發背景補排，`refreshNeighbors` 在 `pageWindow.current` 不變時以 `retainLayoutsForWindow` 保留 `current±1` 的 `_layouts`，`_maybeAutoAdvancePendingNeighbor` 在鄰章 `placeholder` 完成後自動接續翻頁。
- **Viewport 七命令與點擊**：`HybridReaderScreen._attachController:1016` 以 `viewportController.scrollBy/continuousScrollBy/animateBy/moveToNextPage/moveToPrevPage/settleScroll/ensureCharRangeVisible` 掛七閉包，前六個經 `_HybridCommandQueue.enqueue` FIFO 串行（`mounted` 守衛），`settleScroll:1082` 先 `jumpTo(pixels)` 停慣性再 `handleScrollSettled`；`_ensureCharRangeVisibleNow:1174` 對跨窗目標以 `_restoreCore` 重定中心，窗內目標以 `ensureRangeLaidOut` 補齊 `Paragraph + metrics`，再以 `worldRectForRange` 的 `TextBox` 頂底算 `visibleTop/Bottom + topPadding 80 + bottomPadding 120 + preferredTopInset 180` 的 `bounded( minScrollExtent .. maxScrollExtent )` 內 `animateTo(260ms, easeOutCubic)`；`ReaderV2Page._handleContentTap:281` 若 `menu.controlsVisible` 先 `dismissControls()`，否則委 `ReaderV2PageCoordinator.handleTap` 以 `localPosition / (viewport/3)` 九宮格取 `clickActions[row*3+col]` → `ReaderV2TapAction{menu, nextPage, prevPage, nextChapter, prevChapter, toggleTts, bookmark}` 分派，`ReaderV2PointerTapLayer:1` 同為手勢兜底。`ensureCharRangeVisible` 亦為 `ReaderV2PageCoordinator.maybeFollowTtsHighlight` 的唯一驅動：`tts.currentHighlight` 變化時經 `viewportController.ensureCharRangeVisible` 以佇列保證多段高亮循序可見。
- **離場與持久化**：`ReaderV2Page` 以 `ReaderV2PageExitCoordinator:1` 攔截返回：`isDrawerOpen → closeDrawer`，`!book.isInBookshelf && settings.showAddToShelfAlert → shouldPromptAddToBookshelfOnExit 彈「加入書架」`，否則 `persistExitProgress → host.flushProgress()`（`Runtime.flushProgress → ViewportBridge.flushProgress → ProgressController.flush` 清 `_timer` 並 `_flushPendingLocations` 迴圈 `_write` 直至 `pendingLocation==null`），未入架且未保留時 `discardUnkeptBookStorage → BookStorageService.discardBook` 清 `Chapter/Content/Bookmark`；`HybridReaderScreen.didChangeAppLifecycleState:250` 在 `paused/detached/inactive` 時亦 `runtime.flushProgress() + _writeDiskMetrics(snapshot(namespace))`；`ReaderV2ProgressController.dispose:85` 在 `disposed` 後仍 `unawaited(flush())` 保證 debounce 中進度不丟。

## Change Entry Points & Routes
- **新增或調整閱讀頁裝配**：`lib/features/reader_v2/screen/reader_v2_page.dart:30` `ReaderV2Page`（`book/openTarget/initialChapters` 建構參數，`ReaderV2ControllerHost` 與 `ReaderV2PageCoordinator` 的持有、`_buildContent` 的 `LayoutBuilder → HybridReaderScreen` 與 `ReaderV2PageShell` 組裝、`_handleContentTap/_showTts/_showChangeSource/_retryReader`）→ `lib/features/reader_v2/screen/reader_v2_page_shell.dart:1`（`Scaffold/drawer/progressListenable/navigation` 插槽，`chapterTitle/chapterUrl/originName` 顯示）→ `lib/features/reader_v2/screen/reader_v2_controller_host.dart:23`（`ensureRuntime` 的建構與 `syncRuntimeConfiguration` 的 `layoutSignature` 比對與 `contentSettingsGeneration` 監聽）。多檔案路徑：`reader_v2_page.dart` ↔ `reader_v2_controller_host.dart` 的 `isMounted` 閉包 ↔ `reader_v2_session_facade.dart:1` 的 `addCurrentBookToBookshelf`。
- **修改會話狀態與操作語意**：`lib/features/reader_v2/session/reader_v2_state.dart:6` `ReaderV2Phase` 與 `ReaderV2State` 欄位 → `lib/features/reader_v2/session/reader_v2_state_machine.dart:8` 的 `beginOpen/beginJump/beginRestore/beginPresentation/beginContentReload` 與 `isCurrent/completeReady/fail`（`layoutGeneration` 一併鑑別過期操作）→ `lib/features/reader_v2/session/reader_v2_runtime.dart:29` 的 `openBook/applyPresentation/reloadContentPreservingLocation/jumpToChapter/jumpToLocation` 的 `hybridViewportActive` 分流與 `pendingChapterJumpTarget` 的 `identical` 精確清理 → `lib/features/reader_v2/session/reader_v2_navigation_controller.dart:12` 的 `moveToNextPage/moveToPrevPage/jumpToLocation/restoreFromLocation` 對 `PageWindow` 的滑動與 `retainLayoutsForWindow`。新增 `Phase` 需同步更新 `ReaderV2StateMachine._beginOperation` 的 `phase` 映射與 `ReaderV2PageShell` 對 `phase==ready/error` 的 `hasVisibleContent/isLoading` 判定。
- **調整排版規格與引擎**：`lib/features/reader_v2/layout/reader_v2_layout_spec.dart:44` 的 `layoutSignature` 欄位表（含 `kReaderV2CjkTypographyFeatureSignature`）與 `fromViewport` 的 em-grid 鎖寬分支 → `lib/features/reader_v2/layout/reader_v2_style.dart:1` 的 `ReaderV2Style` 欄位 → `lib/features/reader_v2/layout/reader_v2_layout_engine.dart:81` 的 `layout/layoutStep` 與 `_lineStartForbidden/_lineEndForbidden/_isEnglishLetter`、半幀 `_layoutYieldBudget`、`_fitLineChars/_measureLineWidth` → `lib/features/reader_v2/layout/reader_v2_layout.dart:1` 的 `ReaderV2ChapterLayout` 與 `ReaderV2TextLine/ReaderV2PageSlice`。改動 `layoutSignature` 的任一分量皆使 `ReaderV2Resolver` 與 `HybridReaderScreen` 的舊快取失效並觸發首屏重排，需評估冷開成本。
- **調整章節內容管線**：`lib/features/reader_v2/chapter/reader_v2_chapter_repository.dart:29` 的 `_maxContentCacheSize(20)`、`currentChineseConvert` 閉包、`_ensureEnabledReplaceRules` 的 `getEnabledForBook(name, origin)` 與 `getUseReplaceRule()` 守衛 → `lib/features/reader_v2/chapter/reader_v2_content.dart:5` 的 `normalizeRawText` 與 `displayText = title\n\nplainText` 拼接 → `lib/features/reader_v2/chapter/reader_v2_content_transformer.dart:1` 的替換與 `ChineseTextConverter.convert` 調用點 → `lib/core/engine/reader/chinese_text_converter.dart:3` 的 `convertType` 三分支。多檔案路徑：`reader_v2_chapter_repository.dart:237` `_loadViaV2ContentPipeline` 的 `ReaderChapterContentStorage.withMaterializer` ↔ `content_transformer.dart` ↔ `chinese_text_converter.dart`；新增本地書類型需同步 `core/local_book/local_book_formats.dart:1` 的 `isSupportedLocalBookPath` 與 `_loadViaV2ContentPipeline:242` 的 `local://` 檢查。
- **修改混合視口的度量/索引/泵**：`lib/features/reader_v2/hybrid/core/hybrid_types.dart:18` 的 `BlockKey/LayoutEpoch/StyleFingerprint/MeasurementNamespace/BlockMetrics/LayoutTask` 定義 → `lib/features/reader_v2/hybrid/measure/measurement_store.dart:6` 的 `Map<MeasurementNamespace, Map<BlockKey, BlockMetrics>>` 與 `invalidateNamespace/invalidateChapter` → `lib/features/reader_v2/hybrid/measure/document_index.dart:32` 的 `admit/keysInRange/topOf/blockAtOffset/chapterRange` 與雙 `_FenwickTree`（`O(log n)` `prefixSum/firstPrefixGreaterThan`）→ `lib/features/reader_v2/hybrid/pump/layout_pump.dart:13` 的 `measureCellWidth/_buildParagraph/_averageJustifyExpansion/_buildParagraphWithLetterSpacing` 與 `BudgetGovernor/LayoutCostModel` → `lib/features/reader_v2/hybrid/measure/metrics_disk_cache.dart:1` 的 `stableKey` 與磁碟讀寫。`StyleFingerprint` 新增維度需同時更新 `hybrid_types.dart:153` 的 `stableHash/stableKey/==` 與 `layout_pump.dart:32` 的 `_cellWidthCache` 鍵，否則磁碟快取誤命中。
- **調整預載與視窗策略**：`lib/features/reader_v2/session/reader_v2_preload_scheduler.dart:51` 的 `boundaryPreloadPageDistance(4)` 與 `buildCenteredOrder`、雙隊列與 `maxConcurrent=1`、interactive 暫停 → `lib/features/reader_v2/hybrid/view/admission_controller.dart:1` 的 `guaranteedWindow/backwardGuaranteedWindow` 與 `registerChapter/offer/activateViewport/updateLead` → `lib/features/reader_v2/hybrid/view/hybrid_scroll_view.dart:1` 的 `HybridScrollPhysics(admission)`。改動 `boundaryPreloadPageDistance` 或 `guaranteedWindow` 會改變 `HybridReaderScreen._pumpUntilAnchorReady` 的 `initialWindowReady` 判定（`target ± guaranteedWindow`），需與 `_ensureWindowTasks` 的 `[0,1,-1,2,-2]` 探測半徑一併評估。
- **改動進度與 persistence**：`lib/features/reader_v2/session/reader_v2_location.dart:1` 的 `toJson/fromJson/normalized/copyWith` → `lib/features/reader_v2/session/reader_v2_progress_controller.dart:10` 的 `debounce(400ms)`、`saveImmediately/flush/_write`（`BookDao.updateProgress` + `readerAnchorJson`）→ `lib/features/reader_v2/session/reader_v2_viewport_bridge.dart:1` 的 `saveProgress/saveJumpAfterSettled/flushProgress` 與 `OperationToken` 鑑別。改動 `ReaderV2Location` 的欄位即破壞 `readerAnchorJson` 的跨版本相容，需為 `fromJson` 的寬容解析補測試並考慮 `book_dao` 的 `addColumnIfNotExists` 遷移。
- **增刪設定與選單能力**：`lib/features/reader_v2/features/settings/reader_v2_prefs_repository.dart:1`（`SharedPreferences` 鍵集合，`cachedSnapshot` 同步首幀）→ `lib/features/reader_v2/features/settings/reader_v2_settings_controller.dart:10` 的 `readStyleFor/setTypography/setTextIndent/setChineseConvert(set + _contentSettingsGeneration++)/setAutoPageSpeed/setTheme/setMenuTheme/toggleDayNightTheme` → `lib/features/reader_v2/features/settings/reader_v2_settings_sheets.dart:1` 與 `reader_v2_setting_components.dart:1` 的 UI。`setChineseConvert` 的 `generation++` 是觸發 `Host.syncRuntimeConfiguration → reloadContentPreservingLocation` 的唯一信號，新增「影響內容文本」的設定必須同樣遞增該計數，否則切換後內容不重算。
- **調整周邊功能**：`lib/features/reader_v2/features/menu/reader_v2_tap_action.dart:1`（`ReaderV2TapAction{menu,nextPage,prevPage,nextChapter,prevChapter,toggleTts,bookmark}` 與 `fromCode`）、`lib/features/reader_v2/viewport/reader_v2_pointer_tap_layer.dart:1`、`lib/features/reader_v2/features/auto_page/reader_v2_auto_page_controller.dart:1`（`viewportExtent/autoPageSpeed` 閉包，`toggle/isRunning`）、`lib/features/reader_v2/features/bookmark/reader_v2_bookmark_controller.dart:1`、`lib/features/reader_v2/features/tts/reader_v2_tts_controller.dart:1` + `reader_v2_tts_highlight.dart:1` + `reader_v2_tts_sheet.dart:1` + `lib/features/reader_v2/hybrid/overlay/tts_highlight_overlay.dart:1`（`getBoxesForRange → HybridLineBox`）、`lib/features/reader_v2/features/replace_rule/reader_v2_replace_rule_sheet.dart:1` 的 `onReload` 回調。TTS 跟隨依賴 `ViewportController.ensureCharRangeVisible` 的 FIFO 語意，改動佇列順序會使多段高亮跳轉失序。
- **變更排版樣式源**：`lib/features/reader_v2/layout/reader_v2_layout_constants.dart:1` 與 `lib/features/reader_v2/layout/reader_v2_typography.dart:1` 的常量、常量變更需同時更新 `ReaderV2LayoutSpec._buildSignature` 與 `StyleFingerprint.fromLayoutSpec`，否則度量快取誤命中導致錯位。

## Known Risks
- **混合與分頁雙軌的世代漂移**：`ReaderV2Runtime.hybridViewportActive` 以 `registerHybridViewport` 的 `identical(owner)` 判定，`openBook/applyPresentation/reloadContentPreservingLocation/jumpToChapter/jumpToLocation` 皆據此分流——若 `HybridReaderScreen` 在 `dispose` 前未 `unregisterHybridViewport` 或 `didUpdateWidget` 的 `oldRuntime → newRuntime` 遷移遺漏，`hybridViewportActive` 誤為 `false` 會使 `NavigationController` 的 `PageWindow` 邏輯對 `DocumentIndex` 視口生效，兩套錨點座標互不相容且靜默錯位；`ReaderV2LayoutSpec.layoutSignature` 與 `HybridTypes.LayoutEpoch(value=layoutGeneration)` 為兩套獨立的世代（`Resolver._cacheGeneration` 與 `HybridReaderScreen._epoch/_fingerprint`），`bumpGeneration` 與 `invalidateNamespace` 的配對若漏一端會使 `MeasurementStore` 內殘留舊 `namespace` 的度量，後續 `admit` 以相似 `fingerprint` 誤命中而 1px 誤差逐列累積。
- **快取的容量與逐出語意**：`ReaderV2Resolver._maxLayoutCacheSize=50` 與 `ReaderV2ChapterRepository._maxContentCacheSize=20` 為 process 級常駐，`HybridReaderScreen.ParagraphCache(capacity:512)` 每章由 `maxBlockChars = costModel.maxCharsFor(ballisticSliceBudget)` 切出多個 `ChapterBlock`，512 對於超長章節（如 `displayText` > 50k 字）的 `BlockKey` 總數可能不足，`_restorePinning` 僅 pin 錨點與 `updateParagraphPins` 的 `pinRange`，若 `HybridReaderScreen._enqueued.clear()` 與 `ParagraphCache.unpinAll` 的時序在 `pumpPending` 的連續 `completed` 事件之間被打斷，LRU 可能在 `pumpUntilAnchorReady` 完成前逐出首屏後續塊，後續 `DocumentIndex.hitTest` 命中已無 `Paragraph` 的塊而 `captureVisibleLocation` 回 `null`，進度 capture 靜默丟失；`Resolver._inFlight` 以 `'$chapter|sig|generation'` 為鍵，`_invalidatedInFlightTaskIds` 僅在 `updateLayoutSpec/clearCachedLayouts/retainLayoutsFor` 標記而不取消 `Future`，舊排版仍在 `layoutEngine.layoutStep` 中持有 `TextPainter` 並在完成後以 `_throwIfStale` 拋棄結果，若頻繁切換字級會在 UI isolate 上堆積多個 `layoutStep` 的 `halfFrame` 切片，短暫 `jank` 且 `debugLastStats` 被舊切片覆蓋。
- **Capture/Restore 的像素級耦合**：`ReaderV2Location.visualOffsetPx` 限幅 `[-120,120]`，`anchorOffsetInViewport = h*0.2 clamp [24,120]` 在 `ReaderV2LayoutSpec:74`、`AnchorManager:14`、`HybridReaderScreen._offsetForAnchor/_captureVisibleLocation` 三處各自實作（數值一致但無單一常數），改任一處未同步會使 `restore` 的 `top + lineTop - anchorOffset + visualOffset` 與 `capture` 的 `worldY - blockTop - lineTop` 互為逆運算時產生系統偏差，`_lineTopForChar` 與 `_textBoxTopForOffset` 依賴 `Paragraph.getPositionForOffset/getBoxesForRange` 的 `0.1px` 探針偏移，若 `LetterSpacing` 或 `cellWidth` 鎖寬使全形字 `advance` 含小數累積，則 `charOffset → lineTop → visualOffset → charOffset` 往返會因 `clamp` 與浮點捨入產生 ±1 字元漂移，長文連續 `capture/restore` 會逐步累積。
- **替換規則與正文雜湊的順序敏感性**：`ReaderV2ChapterRepository._contentTransformer.process` 在 `ReaderChapterContentStorage.read` 之後執行，`ChineseTextConverter.convert` 在變換尾端執行，`ReaderV2Content.contentHash = sha1(jsonEncode({chapterIndex, title, paragraphs, displayText}))` 包含 `displayText` 的最終形態——若替換規則含捕獲組或 `convertType` 由 0→1 切換，`displayText.length` 變化而 `ReaderV2Location.charOffset` 仍沿用舊 `chapterLength` 鉗制前的舊值，`HybridAnchor.fromLocation` 的 `clamp(0, displayText.length)` 會把舊 `charOffset` 截斷至新文本末尾，跳回該章時落在章末而非原句；`_enabledRules` 以 `getEnabledForBook(name, origin)` 快取於 `_contentCacheGeneration` 世代，`replace_rule_sheet` 的 `onReload` 僅觸發 `reloadContentPreservingLocation` 而未立即 `clearContentCache` 的話（現行已清），舊生成期的規則會在新內容上重放。
- **Viewport 七命令的串行與拖曳互斥**：`HybridReaderScreen._HybridCommandQueue` 對 `scrollBy/continuousScrollBy/animateBy/moveToNextPage/moveToPrevPage/ensureCharRangeVisible` 以 `mounted` 守衛串行，而 `settleScroll` 不經佇列直接 `jumpTo(pixels)` 停慣性，若 `TTS` 的 `ensureCharRangeVisible` 在 `dragging==true` 期間入隊，`pumpPending` 的 `PumpState.dragging` assert（`I4: must not layout while dragging`）會使該命令的 `pumpPending` 零完成並在 `dragging → idle` 恢復後才繼續，`maybeFollowTtsHighlight` 的 `_followingTtsHighlight` 旗標在此期間阻塞後續高亮，朗讀跟隨在拖曳中靜默停步；`_jumpBy/_animateByNow` 的 `_minimumViewportMovement=0.01` 與 `maxScrollExtent` 的 `clamp` 在 `DocumentIndex` 尚未 `admitAll`（`beforeExtent/afterExtent` 仍短）時會誤判 `atForwardBookBoundary/atBackwardBookBoundary` 為 `false` 而不 `emitUserNotice`，書尾/書首提示丟失。
- **進度落盤的時序與離場競爭**：`ReaderV2ProgressController.debounce 400ms` 與 `ReaderV2ViewportBridge.saveJumpAfterSettled(token)` 的「等待當前 `OperationToken` 仍為 `isCurrent` 才落盤」互相競爭——快速連續 `jumpToChapter`（如抽屜連點）會使前一次 `saveJumpAfterSettled` 的 `token` 在 `completeReady` 後已過期，`isCurrent` 為 `false` 而跳過落盤，僅剩 `captureVisibleLocation(notifyIfChanged:true)` 的 `notifyListeners` 路徑觸發 `saveProgress`，若期間 `HybridReaderScreen` 處於 `capturing==true` 而 `Runtime._onRuntimeChanged` 提早 `return`，則該次 capture 的 `notify` 被抑止，進度在離場前 `flushProgress` 才能補回，`dispose` 時的 `unawaited(flush())` 若在 `BookDao.updateProgress` 的 `await` 期間頁面已 `pop`，`Book.chapterIndex/charOffset/visualOffsetPx` 的記憶體鏡像已更新但 DB 仍為舊值（下次啟動以 DB 為準而回跳）。
- **本地書與受支援格式的硬失敗**：`ReaderV2ChapterRepository._loadViaV2ContentPipeline:242` 對 `local://` 且 `isSupportedLocalBookPath==false` 直接拋 `ReaderV2ChapterRepositoryException('本地書格式不受支援')`，而 `MetricsDiskCache` 與 `HybridChapterRepository` 的快取鍵含 `bookUrl`，錯誤路徑下不寫 `Metrics` 也不發 `ChapterEvent.loaded`，`HybridReaderScreen._ensureChapterBlocks → _warmDiskMetricsForChapter` 的 `await` 永不 resolve（`_blocksInFlight` 殘留），後續同一章的 `ensureChapterBlocks` 命中 `inFlight` 而永久掛起，抽屜仍顯示該章但點擊無反應。

## Boundaries
- **`BookOpenRoute` 為唯一開書入口**：`ReaderV2Page` 僅由 `lib/shared/navigation/book_open_route.dart:14` 的 `BookOpenRoute(book, openTarget, initialChapters)` 具現，`openTarget` 的 `ReaderV2OpenTarget.resume/chapterStart` 決定首幀 `anchorOffsetInViewport` 的對齊語意，`initialChapters` 非空時 `ReaderV2ChapterRepository.ensureChapters` 跳過網路而以 `chapterDao.insertChapters` 為準；不可在閱讀器內另建 `ReaderV2Runtime` 或以 `Book.copyWith` 篡改 `bookUrl/origin` 後復用既有 `Runtime`，否則 `sourceDao.getByUrl(origin)` 與 `MetricsDiskCache` 的 `bookUrl` 鍵分叉。
- **`ReaderV2Location` 為跨層座標的唯一契約**：`chapterIndex/charOffset/visualOffsetPx` 的 JSON 形狀 `{chapterIndex, charOffset, visualOffsetPx}` 由 `ReaderV2Location.toJson/fromJson` 與 `Book.readerAnchorJson/bookDao.updateProgress(readerAnchorJson)` 共同約束，`visualOffsetPx` 必須經 `normalizeVisualOffsetPx` 限幅，`charOffset` 必須經 `normalized(chapterLength)` 鉗制；混合與分頁兩套視口的 `capture/restore` 必須經此類型互轉，不可直接以 `pixels/BlockKey/pageIndex` 跨層傳遞，`PendingChapterJumpTarget` 的 `identical` 精確清理依賴此單一類型語意。
- **`layoutSignature` 與 `LayoutEpoch` 的雙重世代隔離**：`ReaderV2LayoutSpec.layoutSignature`（`Object.hash(viewportSize, contentWidth, contentHeight, cellWidth, style.*, kReaderV2CjkTypographyFeatureSignature)`）與 `HybridTypes.LayoutEpoch(value=layoutGeneration)` 分別隔離舊排版快取與舊度量命名空間，`ReaderV2Resolver.updateLayoutSpec` 與 `HybridReaderScreen._handleEpochRebuild/_refreshEpochBinding` 必須成對呼叫（`bumpGeneration/invalidateNamespace/reset DocumentIndex`），不可僅改其中一套而復用另一套的舊 `BlockMetrics/ui.Paragraph`，否則 `cellWidth` 鎖寬的殘差與 `justify` 字距會 1px 累積至錯位。
- **`ReaderV2ViewportController` 七閉包為 Hybrid 與 Coordinator 的唯一操作面**：`scrollBy/continuousScrollBy/animateBy/moveToNextPage/moveToPrevPage/settleScroll/ensureCharRangeVisible` 的簽章與 FIFO 語意（`settleScroll` 直達）為 `ReaderV2PageCoordinator` 與 `ReaderV2AutoPageController`/`ReaderV2TtsController` 的契約，非 hybrid 分支的 `Runtime.moveToNextPage/moveToPrevPage` 僅作無 viewport 的 fallback，不可同時驅動兩套翻頁路徑；`ensureCharRangeVisible` 的 `chapterIndex/startCharOffset/endCharOffset` 為 TTS/選詞/搜尋跳轉的統一入口，改其語意需同步 `HybridReaderScreen._ensureCharRangeVisibleNow` 的跨窗 `restore` 分流與 `getBoxesForRange` 的 `0..displayText.length` 鉗制。
- **`HybridLayoutPump` 為唯一可建 `ui.Paragraph` 的實體（I4）**：`DocumentIndex/MeasurementStore/ParagraphCache/AdmissionController` 均不可直接 `ParagraphBuilder.build/layout`，全部經 `LayoutPump.submit → pumpPending → put(paragraphCache, measurementStore) → completed → admission.offer` 管線；`pumpPending` 在 `PumpState.dragging` 時必須零完成並以 `assert` 攔截，違規會使拖曳幀內 `Paragraph.layout` 觸發 `jank` 並污染 `BudgetGovernor.hasLeadDeficit`。
- **`I2/I3` 的連續 admit 不變量**：`DocumentIndex.admit` 要求每側自 `centerKey` 向外單調連續（亂序以 `assert` 攔截並 `rebuildAll` 回退），`HybridReaderScreen._admitOrSubmit` 在 `blocked` 後禁止同側後續 `direct-admit`（否則中間洞在補齊時使可見內容位移），`HybridReaderScreen._handleEpochRebuild` 必須 `measurementStore.invalidateNamespace + documentIndex.reset(centerKey)` 原子化，不可保留舊 `DocumentIndex` 的 `beforeExtent/afterExtent` 而僅換 `MeasurementStore`。
- **中文排版與字體鏈的固定契約**：`ReaderV2LayoutEngine._lineStartForbidden/_lineEndForbidden` 與 `kReaderV2CjkFontFeatures/kReaderV2CjkTypographyFeatureSignature`、`LayoutPump._cellWidthSignature`（`fontSize|letterSpacing|bold|signature`）與 `em-grid` 實測 `cellWidth` 構成 CJK 排版的視覺基線，`ReaderV2LayoutSpec.fromViewport` 的 `cells*cellWidth+0.05` 與 `sidePadding` 回填、`LayoutPump._buildParagraph` 的 `placeholder(indentCellWidth)` 與兩段式末行補償、`ReaderV2LayoutEngine._isEnglishLetter` 的單字防切斷，皆為 `ReaderV2Content.displayText` 的座標系前提，改任一常量需全鏈路回歸「字數建議預算 `maxBlockChars`→`DocumentIndex` 長度→`anchor` 換算」的像素級對齊。
- **章節章序與去重語意**：`WebBook.getChapterListAwait` 的去重與 `isReverse`/`reverseToc` 雙重反轉、`ReaderV2ChapterRepository.ensureChapters` 的 `insertChapters` 自增 `index`，均為 `BlockKey.chapterIndex/ChapterBlocks.blockForCharOffset` 的順序前提；`HybridReaderScreen._ensureWindowTasks` 的 `[0,1,-1,2,-2]` 與 `PreloadScheduler.scheduleAround(radius:1)` 的半徑需一致，否則視口可見章節與預載章節的交集在邊界產生洞（`DocumentIndex.keysInRange` 的對應範圍為可見幀提供 `O(k log n)`，洞會使 `hitTest` 回 `null`）。
